# 场景精修计划 — 从"Godot 画出来的"到"成品游戏"

> 这份是**主计划**。目标是**实际游戏体验**：世界看起来、动起来像一个做完的游戏。
>
> 2D 界面延后，见 [UI-POLISH.md](UI-POLISH.md)。
>
> 交接单写法：每条任务冷启动可做、只碰一小块、做完能自证、**必须出截图**。一次派一条。

---

## 第 0 节：诊断 — 现在为什么不像成品

三件事，按严重程度排：

### 1. 一个会动的东西都没有

**整个项目零行动画代码。** 没有 `AnimationPlayer`，没有 `AnimationTree`，没有任何驱动。人、恐龙、栅栏全是僵直的雕像，滑着移动。

**这是差距最大的一条，比贴图重要得多。** 一头不会动的恐龙比一个不会动的方块更难看——方块不承诺自己是活的，恐龙承诺了。

而且**仓库里早就躺着一个带骨架、带 5 个动画的 `assets/models/t_rex.glb`**（`tools/generate_dino.py` 生成的），**从来没有被接进游戏过**。验证动画管道的成本比看上去低得多。

### 2. 所有模型是代码逐个三角形生成的

`ModelLibrary.gd` / `GroundCover.gd` / `TerrainBuilder.gd`：没有 Blender，没有资产包，**没有任何贴图，只有顶点色**。

代码能做比例、剪影、颜色。**做不出**织物纹理、鳞片层次、金属磨损、树皮沟壑——而"精致"恰恰由这些构成。**这条路到头了，唯一的出路是换成真资产。**

### 3. 混搭资产是第二杀手

换成买来的资产之后，最容易翻车的地方是**东拼西凑**：不同作者的 texel 密度不同、photoscan 色彩分级不同、尺度约定不同。**一堆各自精致的模型放在一起会比一堆统一的方块更难看。**

所以本计划有两条铁律和一条终审任务（S17）专门管这件事。

---

## 第 1 节：铁律（每条任务都要守）

1. **授权必须干净，且必须记录。** 只用 CC0 / CC-BY / 明确允许商用的资产。每引入一个资产，在 `assets/CREDITS.md` 写下：来源 URL、作者、许可、下载日期。**没有记录的资产等于不能发布的资产**，而这件事必须在引入当天做，事后无法补。
2. **同一类别，同一来源。** 所有恐龙尽量出自同一个包；所有植被出自同一个包；所有岩石出自同一个包。宁可用一个差一点但统一的包，也不要东拼西凑。
3. **尺度从 Config 来，不从模型来。** 接入管道已经修好了：`Config.VISUALS[key].scene` 填路径即可，`VisualLibrary.fit()` 会把任何比例、任何原点的模型按声明尺寸装配好。**不要在模型上手调 scale。**
4. **碰撞永远不从美术反推。** 碰撞体从声明尺寸来。美术可以比碰撞小，**绝不能比碰撞大**——那会变成"看得见却拦不住 / 看不见却拦得住"，这个项目已经为此返工过三轮。
5. **每条任务必须出截图。** 改了画面没看过，等于没做。

### 收尾三件套

```bash
# 全量测试必须全绿
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --script res://tests/test_runner.gd

# 出图（含近景）
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --path . --resolution 1600x900 --script res://tools/playtest.gd

# 开图冒烟，不许有报错
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --quit-after 400
```

> **跑测试要连 stderr 一起看。** GDScript 的**运行时错误不会让测试失败**——它只是把那个测试函数中断掉，而 runner 只看断言，于是"已经断言过几条"的测试照样报 PASS。发现的时候有两个测试**沉默地半途而废**了好几个版本。所以：
>
> ```
> "C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --script res://tests/test_runner.gd 2>&1 | grep -c "SCRIPT ERROR"
> ```
>
> **这个数必须是 0**，和 `ALL TESTS PASSED` 同等重要。

新增 `class_name` 或改 `strings.csv` 之后**必须**先跑 `--headless --editor --quit`。

**分支**从 `main` 开，名字见任务表。**不要 push，不要合并。**

---

## 第 2 节：去哪找资源（给 agent 的起点）

按"授权干净 + 风格统一"排序。**优先在前几个里找齐，不要一上来就 Sketchfab 东挑一个西挑一个。**

| 来源 | 许可 | 有什么 | 备注 |
|---|---|---|---|
| **Quaternius** | CC0 | **有整包恐龙**、植被、岩石、角色 | 风格统一、带骨架动画、**这个项目的首选** |
| **Kenney** | CC0 | 自然、角色、道具 | 风格统一度极高，偏低多边形 |
| **Poly Haven** | CC0 | **HDRI 天空**、PBR 贴图、部分模型 | HDRI 是 S8 的答案，贴图是 S7 的答案 |
| **ambientCG** | CC0 | PBR 材质（土、草、岩石、金属） | 地形与模型材质的主力 |
| **Godot Asset Library** | 各异 | 着色器、工具、少量资产 | 风、水、描边着色器可以在这找 |
| **Sketchfab** | 需过滤 | 什么都有 | **必须筛 CC0/CC-BY 且逐个记录许可**，风格最难统一，最后再用 |
| **itch.io** | 各异 | 成套资产包 | 有不少高质量免费包，注意商用条款 |

**格式**：优先 `.glb`（Godot 原生支持，带骨架动画）。`.fbx` 需要转换。**不要引入需要付费插件才能用的格式。**

---

## 第 3 节：任务表

**S1 和 S2 是地基，必须最先做，且按顺序。** 之后 S3–S16 大体可并行，依赖见表。**S17 必须最后做。**

| # | 任务 | 依赖 | 分支 |
|---|---|---|---|
| **S1** | **资产选型与授权台账** | — | `scene-asset-survey` |
| **S2** | **动画状态机接口**（代码地基） | — | `scene-animation-rig` |
| S3 | 恐龙：模型 + 动画接入 | S1 S2 | `scene-dino` |
| S4 | 人：模型 + 动画接入 | S1 S2 | `scene-hero` |
| S5 | 飞船残骸 | S1 | `scene-wreck` |
| S6 | 植被：树、蕨、草 | S1 | `scene-vegetation` |
| S7 | 地形材质：PBR 贴图多层混合 | S1 | `scene-terrain-material` |
| S8 | 天空：HDRI 环境 | S1 | `scene-sky` |
| S9 | 岩石与巢穴 | S1 | `scene-rocks-nest` |
| S10 | 木栅栏与防御塔 | S1 | `scene-buildings` |
| S11 | 风：植被摆动 | S6 | `scene-wind` |
| S12 | 粒子特效 | — | `scene-particles` |
| S13 | 贴花：脚印、焦痕 | — | `scene-decals` |
| S14 | 水面着色器 | — | `scene-water` |
| S15 | 摄像机手感 | — | `scene-camera-feel` |
| S16 | 光照与后处理终调 | S8 | `scene-lighting-final` |
| **S17** | **风格审计**（一致性终审） | 全部 | `scene-style-audit` |
| S18 | 性能：LOD 与实例化 | S3–S10 | `scene-performance` |

---

## S1 — 资产选型与授权台账

**分支**：`scene-asset-survey` ｜ **最先做，它决定后面所有任务的输入**

### 目标

**这一条不写代码。** 它的产出是一份**决定**：每个类别用哪个包，以及为什么。

混搭资产是这个阶段最大的翻车方式（第 0 节第 3 条），而混搭是**一次一个决定慢慢滑进去的**。先把选型定死，后面每条任务就只是执行。

### 要做的事

1. 按第 2 节的来源清单，为这几类各找 **2–3 个候选**：恐龙、人形角色、植被、岩石、地形 PBR 贴图、HDRI 天空。
2. 每个候选记录：URL、作者、许可、格式、面数量级、**是否带骨架动画**、风格描述。
3. **出一份推荐**：每类选一个，写明理由，尤其是**跨类别之间风格能不能对得上**（这是选型的核心，不是单个包好不好）。
4. **下载并试装一个恐龙**，用 `Config.VISUALS` 接进去截一张图——**证明管道通，且风格在游戏自己的相机下成立**。
5. 建立 `assets/CREDITS.md`，格式定下来。

### 验收

- `assets/CREDITS.md` 存在，格式清晰，至少有一条真实记录。
- 一份 `docs/asset-选型.md`（或写在 CREDITS 里）列出候选与推荐。
- **一张截图**：试装的恐龙站在当前场景里。
- 测试：新增一条断言 `assets/CREDITS.md` 存在且非空（挡住"忘了记许可"）。

### 不在范围内

- 不要把所有资产都接进去（那是 S3–S10）。
- 不要买付费资产，先把免费的可能性走完。

---

## S2 — 动画状态机接口

**分支**：`scene-animation-rig` ｜ **代码地基，和 S1 可并行**

### 目标

**整个项目零行动画代码。** 人和恐龙在代码里早就有状态（`Hero.State`：IDLE / MOVING / BUILDING / HARVESTING / ATTACKING / DEAD；`Dino` 同理），但**没有任何地方驱动动画**。

建一层接口：**实体报告自己的状态，这一层决定播什么**。实体不许知道动画剪辑的名字。

### 要改的文件

- `scripts/fx/ActorAnimator.gd`（新建）
- `scripts/autoload/Config.gd`（状态 → 剪辑名的映射表）
- `scripts/entities/Hero.gd`、`scripts/entities/Dino.gd`（上报状态，**不要写剪辑名**）
- `tests/test_scene_animation.gd`（新建）

### 要做的事

1. `ActorAnimator` 挂在实体上，找到模型里的 `AnimationPlayer`，按当前状态播对应剪辑。
2. **状态到剪辑的映射写在 Config**，因为不同资产包的剪辑名不一样（`Walk` / `walk` / `Armature|Walk`），换包时改数据不改代码。
3. **必须优雅降级**：模型没有 `AnimationPlayer`、或者少某个剪辑时，**不许报错、不许卡住**——退回静止。现在大部分模型还是程序化的，所以这条是常态不是边缘情况。
4. 过渡要有混合时间，不要硬切。
5. **用 `assets/models/t_rex.glb` 验证**——它带骨架和 5 个动画，是现成的试验台，而且从来没被用过。

### 验收

- 断言：状态改变时请求的剪辑名随之改变（从 Config 映射推导，不复述字符串）。
- 断言：**没有 `AnimationPlayer` 的实体照常工作**，不报错（这条最重要）。
- 断言：Config 里映射的每个状态都在实体的状态枚举里，没有拼错的死条目。
- 截图：t_rex 接进去、播行走动画的一帧。

### 不在范围内

- 不要做动画混合树、IK、程序化摆动（以后再说）。
- 不要改任何实体的状态机逻辑——只读状态，不改状态。

---

## S3 — 恐龙：模型 + 动画接入

**分支**：`scene-dino` ｜ **依赖 S1 S2**

### 目标

`Config.DINOS` 有三种：`raptor`、`big_theropod`、`pterosaur`。它们现在共用一个程序化模型，只有颜色和尺寸不同。**恐龙是这个游戏的名字，它们必须是最精致的东西。**

### 要做的事

1. 按 S1 的选型接入模型，**每种恐龙一个 `Config.VISUALS` 条目**，`scene` 填路径。
2. 接上 S2 的动画：至少 **idle / walk / attack / death** 四个状态。
3. **尺度核对**：`Config.DINOS[type].size` 是声明尺寸，`VisualLibrary.fit()` 会装配。**确认大型兽脚类真的比迅猛龙大**（这条有测试盯着：`test_v05_visual_library.gd::test_06`）。
4. 翼龙如果包里没有，记录下来，先用近似的。

### 验收

- 断言：三种恐龙的 `VISUALS.scene` 都非空且文件存在。
- 断言：`test_v05_visual_library.gd` 全绿（尺寸不变量）。
- 断言：每种恐龙都能找到 idle 与 walk 剪辑，否则**明确降级**而不是报错。
- 截图：三种恐龙近景各一张（`playtest.gd -- closeup`）+ 一张来袭中的战场。
- `assets/CREDITS.md` 已更新。

---

## S4 — 人：模型 + 动画接入

**分支**：`scene-hero` ｜ **依赖 S1 S2**

### 目标

人是玩家**唯一操作的单位**，全程盯着他。现在是一堆方块。

### 要做的事

1. 接入人形模型 + 骨架。
2. 动画覆盖他的全部状态：**idle / walk / build（挥锤）/ harvest（砍伐）/ attack / death**。建造和采集这两个是这个游戏的主要玩法动作，**不能用 idle 糊弄过去**。
3. 尺度：`Config.HERO.width` / `height` 是声明尺寸。

### 验收

- 断言：`Hero.State` 的每个状态在 Config 映射里都有对应剪辑（**不许有状态没人管**）。
- 截图：站立、行走、建造、采集四张近景。
- `assets/CREDITS.md` 已更新。

---

## S5 — 飞船残骸

**分支**：`scene-wreck` ｜ **依赖 S1**

### 目标

残骸是**故事物件**：人穿越过来的唯一证据，被啃掉就输。它是整张地图的视觉焦点。

它还必须一眼看出是**制造出来的**——画面里别的东西全是有机的，这个反差比任何细节都更说明"它不属于这里"。

### 要做的事

1. 找或组合出一个**坠毁的现代载具**。可以是飞船、舱段、舱体残骸。**金属 + 烧灼 + 变形**。
2. 尺度：现在声明 `footprint` 默认 1.0、`height` 1.9。**注意：footprint 超过约 1.2 会触发 `is_barrier_building`，把基地变成一堵墙**（见 `Config.BUILDINGS.core` 的注释）。要放大得先弄清这条。
3. 周围可以散落碎片（纯装饰，不碰碰撞、不上格子）。

### 验收

- 断言：`is_barrier_building("core")` 的结果**没有变**。
- 断言：美术的水平投影不超出它的碰撞盒。
- 截图：近景 + 游戏视角各一张。

---

## S6 — 植被：树、蕨、草

**分支**：`scene-vegetation` ｜ **依赖 S1**

### 目标

现在草、蕨、树是程序化三角形。植被占画面面积最大，**它的质量直接等于场景的质量**。

### 要做的事

1. 换成资产包的植被，**必须是同一个包**（第 1 节铁律 2）。
2. **保留 MultiMesh**：现在草是 9000 株一个 draw call（`GroundCover.scatter`）。换成真模型后**面数会暴涨**，必须保持实例化，并考虑降低密度或加 LOD。
3. **保留"分布不许有硬边"**这条——地被要越过可玩区继续铺开并渐稀（这个 bug 犯过一次）。
4. 树（`node/wood`）要有**完整 / 采空**两个状态。

### 验收

- 断言：草和蕨仍然走 `MultiMeshInstance3D`（不是几千个独立节点）。
- 断言：地被分布仍然延伸过 `field_half`（`Config.TERRAIN.cover_reach`）。
- 断言:植被不参与碰撞、不上格子。
- **性能**：记录换之前/之后的帧率，写在 commit 里。
- 截图：广角 + 近景。

---

## S7 — 地形材质：PBR 贴图多层混合

**分支**：`scene-terrain-material` ｜ **依赖 S1**

### 目标

地面现在是顶点色 + 程序化噪声细节。换成**真 PBR 贴图**（albedo / normal / roughness / AO），并且**按坡度混合**：平地是土草，陡坡是岩石。

坡度混合的逻辑**已经有了**（`TerrainBuilder._ground_colour`），现在是混颜色，要改成混贴图。

### 要做的事

1. 从 ambientCG / Poly Haven 取两到三套材质：草地、泥土、岩石。
2. 写一个着色器按坡度（和高度）混合它们，**三平面投影**（地形网格没有 UV，而且陡坡上平面投影会拉伸）。
3. **可玩区必须保持绝对平**（`TERRAIN.field_half`，有测试盯着）——这条只改外观。

### 验收

- 断言：`test_v05_terrain_shape.gd` 全绿（平坦不变量）。
- 截图：谷底、山坡、交界处三张。
- `assets/CREDITS.md` 已更新。

---

## S8 — 天空：HDRI 环境

**分支**：`scene-sky` ｜ **依赖 S1**

### 目标

现在是 `ProceduralSkyMaterial`——一个渐变。换成 HDRI，**这会同时改善天空和整个场景的环境光**，因为环境光源是天空。

**在 PBR 里这是单条性价比最高的改动**：所有材质的反射和环境光一次全对。

### 要做的事

1. 从 Poly Haven 取一张 HDRI。**选低太阳角度的**（清晨/傍晚），侧光是让地形起伏看得见的东西；正午顶光会把一切拍平。
2. 设成 `Environment.background_mode = BG_SKY` + `PanoramaSkyMaterial`。
3. **平行光的方向要和 HDRI 里的太阳对上**，否则阴影方向和天空打架，这是最容易被忽略又最毁真实感的错。
4. **注意船舱**：它有自己的 `Camera3D.environment`（`CabinInterior._interior_environment`），不要被这条改动波及。

### 验收

- 断言：`test_v05_environment.gd` 全绿。
- 断言：船舱内景仍然不显示天空。
- 截图：户外 + 船舱各一张。
- `assets/CREDITS.md` 已更新。

---

## S9 — 岩石与巢穴

**分支**：`scene-rocks-nest` ｜ **依赖 S1**

- 石头资源点（`node/stone`）：**完整 / 采空**两态。
- 巢穴：恐龙从这里涌出来，**洞口必须从游戏视角一眼看得出**。
- 散落的碎石（`GroundCover.stone`）可以一并换。

**验收**：断言两态几何不同（不只是颜色）；截图近景 + 游戏视角。

---

## S10 — 木栅栏与防御塔

**分支**：`scene-buildings` ｜ **依赖 S1**

### 目标

木栅栏是玩家造得最多的东西。它现在是程序化圆锥——**而且它的形状逻辑是返工过三轮的地方，不要碰逻辑**。

### 要做的事

1. 换成真模型，但**必须保留自动拼接**：成排是一条线、拐角是 L、单根是十字（`Wall.axis_at` + `Config.spike_offsets`）。**只换每根尖刺的模型，不要换排布规则。**
2. 防御塔要读得出是"人造的、能打的"。

### 验收

- 断言：`test_v04_terrain.gd` 的 test_11–test_18 **全绿**（栅栏形状与预览一致性）。
- 截图：单根、成排、拐角、成片四张。

---

## S11 — 风：植被摆动

**分支**：`scene-wind` ｜ **依赖 S6**

### 目标

**静止的植被是"这是个渲染"最强的信号。** 让草和树动起来，场景立刻活。**这条的投入产出比极高。**

### 要做的事

1. 顶点着色器摆动，按高度加权（根部不动、梢部摆动最大）。
2. **必须兼容 MultiMesh**，并且用实例位置做相位偏移，否则上万株草会整齐划一地摆，比不动还假。
3. 风的参数进 Config。

### 验收

- 断言：风参数来自 Config。
- 截图：**两张相隔若干帧的图**，看得出草的姿态不同。

---

## S12 — 粒子特效

**分支**：`scene-particles`

现在只有 `Fx.debris()` 一个方块碎屑。要加：脚步扬尘、受击、死亡、建造木屑、采集碎屑。

**约束**：全部走 `GPUParticles3D`；数量进 Config；**不许影响玩法判定**。

**验收**：断言粒子不在任何碰撞层；截图每种特效一张。

---

## S13 — 贴花：脚印、焦痕

**分支**：`scene-decals`

恐龙走过留脚印、残骸周围有焦痕、战斗处有痕迹。用 `Decal` 节点。

**约束**：贴花有上限并回收（不能无限累积）；**不参与碰撞**。

**验收**：断言贴花数量有上限且会回收；截图一波来袭之后的地面。

---

## S14 — 水面着色器

**分支**：`scene-water`

水潭（`node/water`）现在是一个深色圆盘。做成有波纹、有折射、有边缘泡沫的水面。

**注意**：水目前**没有任何消耗途径**（VERSION.md v0.4 待定第 1 条）。它仍然在画面里，所以值得做，但别为它加玩法。

**验收**：截图近景；断言水面不碰碰撞。

---

## S15 — 摄像机手感

**分支**：`scene-camera-feel`

现在是完全静止的固定相机。加：受击/爆炸时的轻微震动、平滑跟随、缩放的缓动。

**约束**：震动幅度进 Config 且**必须能关**（有人对镜头晃动敏感）；**不许改相机的角度和投影**。

**验收**：断言震动可关闭；断言默认相机角度未变。

---

## S16 — 光照与后处理终调

**分支**：`scene-lighting-final` ｜ **依赖 S8**

所有资产接完之后，对着真实画面把 `Config.ENVIRONMENT` 重调一遍。

**硬约束（有测试盯着）**：`fog_depth_*` 只在 `FOG_MODE_DEPTH` 下生效；`fog_density` 在两种模式下含义完全不同。见 `tests/test_v05_environment.gd::test_06b`。

**验收**：环境测试全绿；改前/改后同机位对比图。

---

## S17 — 风格审计（终审）

**分支**：`scene-style-audit` ｜ **必须最后做**

### 目标

**这条是整个计划的验收本身。** v0.5 的标准从来不是"每个模型好看"，而是**八样东西放在同一个画面里，看起来像出自同一个游戏**。

混搭资产的问题只有在全部接完之后才看得出来。

### 要做的事

1. 截一张**八样同框**的图：恐龙、船舱（残骸）、人、木栅栏、树、地形、石头、巢穴。
2. 逐项检查：**texel 密度**是否接近、**色彩分级**是否统一、**面数档次**是否协调、**尺度**是否合理（人和恐龙和树的相对大小说得通吗）。
3. 找出最跳戏的两三样，**要么统一它们，要么记录为待办**。
4. 更新 VERSION.md 的 v0.5 交付记录。

### 验收

- **一张八样同框的截图**——这就是 v0.5 的验收标准（VERSION.md 原话：验收标准是一张截图，不是一个清单）。
- 一份简短的审计说明：哪些统一了，哪些没有，为什么。

---

## S18 — 性能：LOD 与实例化

**分支**：`scene-performance` ｜ **依赖 S3–S10**

真模型的面数比程序化几何高一到两个数量级。

**要做的事**：植被 LOD 与剔除距离、阴影距离、MultiMesh 保留、必要时合批。

**验收**：记录改前/改后帧率；断言性能相关参数来自 Config。

---

## 加任务的格式

**目标 / 为什么是独立的一条 / 要改的文件（以及不许碰的）/ 要做的事 / 验收（含截图）/ 不在范围内 / 分支名 / 依赖。**

「不许碰的文件」和「依赖」不是客套——它们是这份单子能并行派活的全部原因。

# Defend Dinosaur — 版本记录与开发规划 (Version History & Roadmap)

本文档作为项目的外部版本说明与演进规划，记录已交付版本的特性清单，并维护后续版本的迭代 TODO。

---

## 目录
- [当前版本](#当前版本)
- [故事背景](#故事背景)
- [v0.0 核心闭环原型交付清单](#v00-核心闭环原型交付清单)
- [v0.1 迭代规划与 TODO 清单](#v01-迭代规划与-todo-清单)
- [v0.2 迭代规划与 TODO 清单](#v02-迭代规划与-todo-清单-roadmap)
- [v0.3 迭代规划与 TODO 清单](#v03-迭代规划与-todo-清单)
- [v0.4 迭代规划与 TODO 清单](#v04-迭代规划与-todo-清单)
- [v0.5 迭代规划与 TODO 清单](#v05-迭代规划与-todo-清单)
- [v0.x 远期构想（不排期，仅记录）](#v0x-远期构想不排期仅记录)
- [版本号构建与注入规范](#版本号构建与注入规范)

---

## 当前版本
- **版本号**：`v0.5.0`（唯一来源：`project.godot` 的 `application/config/version`，**现在由测试盯着**：见 `tests/test_v05_version_and_settings.gd`）
- **构建阶段**：Alpha / Feature Delivery (Real-Time Deployment & Modern Hero)
- **目标引擎**：Godot 4.7 (Forward+ / Jolt Physics)
- **交付时间**：2026-09-16

---

## 故事背景

一个**现代人**——一位很强的建造大师——穿越到了远古时代。他要在恐龙环伺的环境里活下来，同时找到回去的方法。

**现代废弃船舱**是他穿越回去的唯一证据，舱内保存着关键信息，必须受到保护。它在玩法上就是核心：

- 船舱不可移动，是玩家的起点与整条防线的中心；
- 玩家的全部建筑技术都建立在船舱内的信息之上；
- **恐龙攻陷船舱 = 游戏结束**（对应 `game_lost`）。

> **命名映射**：v0.0 中该角色名为"营火"（代码 id `core`，实体脚本 `CoreCampfire.gd`）。v0.1 起**界面与文档文案统一改为"废弃船舱"**，内部 id、HP 逻辑与失败判定完全不变——这是一次纯改名，不含任何机制改动。本文档的 v0.0 交付清单保留"营火"字样以忠实记录当时的交付内容。

---

## v0.0 核心闭环原型交付清单

### 1. 架构骨架与解耦设计
- **三大 Autoload 单例**：
  - `Config`：统一集中管理所有数值平衡、建筑属性、恐龙属性、波次倍率与网格常量，全代码无任何业务硬编码。
  - `EventBus`：定义 15 个强类型全局事件信号，彻底解耦场景、UI、战斗、波次与经济模块。
  - `GameState`：运行时状态机（AP 事务管理、多资源仓储、波次推进、终态竞争锁死机制）。
- **坐标网格与建筑放置**：
  - `GridManager`：提供四象限世界空间坐标与离散网格单元的双向换算，精准映射格子中心与边界。
  - `BuildSystem`：原子级建造校验（空地检测、AP 与木材资源扣减、撤销与返还联动）。
  - **连续建造交互**：支持在拥有足够资源与 AP 时连续放置建筑，支持鼠标右键或 ESC 随时退出建造模式。

### 2. 回合循环与经济系统
- **状态机循环**：`PLAN`（规划建造）→ `ATTACK`（自动防守战斗）→ `PRODUCE`（结算产出）→ `PLAN`。
- **AP 消耗与提示透明化**：
  - 基础 AP（3点/天），建造建筑均扣除 1 AP。
  - HUD 按钮明确标注 AP 与资源成本（如 `木墙 (2木, 1AP)`）。
  - AP 或资源不足时弹出动态高亮文字指引。
- **自动生产结算**：进攻阶段结束后，自动触发 `PRODUCE` 阶段，场上存活的生产建筑（如伐木屋）自动发放木材，并在延迟后平滑切回新一轮 `PLAN` 阶段重置 AP。

### 3. 恐龙实体与防线战斗
- **路径导航与障碍撕咬**：
  - 恐龙沿路径点（Marker3D）序列朝核心营火行进。
  - 前向多光线（Multi-Ray）与体积重叠检测：遇阻挡建筑（木墙、自动哨位、核心营火）立即停步并切换为攻击状态。
- **防长龙多道散兵方阵**：
  - **3车道错落推进**：生成时按 `-0.35`、`+0.35`、`0.0` 轮换车道，自然形成散兵方阵推进，消除“排成单列长龙/长条矩形”的视觉缺陷。
  - **横向软排斥分离**：恐龙间保留 $1.15\text{m}$ 安全车距（$0.35\text{m}$ 清晰视觉间隔），遇阻并排站立攻击。
  - **道路走廊约束**：恐龙严格限制在道路 $[0.4, 1.6]$ 范围内移动，绝不掉出路面，且与路旁建筑保留物理缓冲区。
- **防御塔阻挡与射击**：
  - 移除对 `tower` 的排除判定，道路上的防御塔作为有效障碍物被恐龙阻挡撕咬（防穿模）；
  - 哨位在 5.0m 范围内自动索敌并以 1.0s 周期射击最近目标。

### 4. 胜负闭环与用户界面
- **胜利条件**：自动哨位射程推至恐龙巢穴，巢穴 HP 归零触发 `game_won`。
- **失败条件**：核心营火 HP 被恐龙攻击归零触发 `game_lost`。
- **终态锁定**：胜负判定后锁死操作与状态，杜绝后期伤害造成的竞争条件覆盖。
- **响应式 HUD 面板**：实时显示 AP、木材、波数、营火 HP、当前阶段与版本号（`v0.0`），提供游戏安全重开（Restart）功能。

### 5. 质量工程保证
- **全量自动化测试套件**：涵盖骨架、网格、建造、回合、波次、战斗、胜负、HUD 重启等 20 个测试套件，共计 345 项测试、15,137 条断言，在 Godot 4.7 控制台 Headless 下 100% 绿色通过。

---

## v0.1 迭代规划与 TODO 清单

> **范围说明**：v0.1 的必做项只有第 1、2 节（实时部署 + 现代人 + 巢穴 / 恐龙集群寻路）。第 3~5 节为候补项，做不完顺延到 v0.2，不影响 v0.1 交付判定。
>
> **本节是 v0.1 唯一的规则级改动，其余一切照旧。** 定稿前曾评估过"体力值"与"每回合固定移动格数"两种方案，均已否决：体力按距离扣减会惩罚玩家试探走位；固定格数本质仍是回合制，没有时间感。最终采用**实时部署 + 计时来袭**。

### 1. ★★ 实时部署、现代人与巢穴 (Priority 0 — v0.1 必做)

- **设计动机（v0.0 暴露的玩法漏洞）**：
  - v0.0 沿用了设计文档第 8 节的默认决策"建造不要求角色走到目标格（点哪建哪）"，导致行动点与空间距离完全无关——玩家可以开局直接把哨位摆在恐龙巢穴脸上，巢穴瞬间被清，胜利条件形同虚设；地图越大该漏洞越严重。
  - 根因是**行动点是离散的、与空间无关的**。v0.1 把唯一货币从行动点换成**时间**，并在巢穴外布置守卫恐龙：走路与施工都花时间，靠近巢穴还会被守卫攻击。距离阻碍由此自然成立，无需任何额外的领土或体力规则。

- **一天的结构（v0.0 三阶段状态机原样保留，只改名并绑定时段）**：
  | v0.0 阶段 | v0.1 阶段 | 时段 | 改动 |
  | :--- | :--- | :--- | :--- |
  | `PLAN` | **部署 DEPLOY** | 白天 | 加实时倒计时；玩家操作现代人移动、建造、攻击 |
  | `ATTACK` | **来袭 ATTACK** | 白天 | 现代人隐藏；仅建筑防御；恐龙从巢穴出来沿路径进攻 |
  | `PRODUCE` | **产出 PRODUCE** | 夜晚 | 逻辑无改动（生产建筑结算），结束后进入次日部署 |

- **已定设计规则**：
  1. **行动点（AP）彻底取消，现代人拥有无限 AP**：时间是唯一行动货币，不再有第二种离散行动点。玩家在部署阶段只要持有足够木材即可连续建造，建造仅受木材储量与就位施工耗时制约。
  2. **部署阶段是实时的**：`deploy_length` 秒倒计时，归零自动进入来袭。HUD 显示剩余部署时间，隐藏 AP 显示。
  3. **现代人**（场上唯一可控化身，对应设计文档 B2，从"锦上添花"升级为核心依赖）：方块占位，点击地面实时走过去（`move_speed` 米/秒），走路本身不消耗任何资源，**只消耗部署时间**。
  4. **就位建造 + 施工耗时**：选中建筑类型 → 点击目标格 → 现代人自动走过去 → 站在 `build_range` 内施工 `build_time` 秒（每种建筑各自定义）→ 完工。资源在**开工时**扣除。
  5. **暂停允许，但施工与移动同时暂停**：玩家随时可以暂停思考、查看局势、规划建造顺序，但暂停期间时间不流动、工程不推进——保住"可以慢慢想"的手感，又不能靠暂停偷时间。
  6. **可提前结束部署**：部署阶段没事干时点按钮直接进入来袭，不必干等倒计时。
  7. **现代人只在部署阶段能攻击**：可攻击巢穴外的守卫恐龙。来袭阶段不能攻击（见规则 8）。
  8. **来袭阶段现代人隐藏**：进入来袭瞬间现代人从场上隐藏——不可见、不可操作、不可被攻击、不参与任何战斗判定。**这一阶段只有建筑在防御。**
  9. **未完工建筑 = 半成品**：保留施工进度，**不提供任何功能**（塔不开火、伐木屋不产出），且**在来袭阶段不吸引恐龙仇恨**。实现上未完工期间**不启用碰撞体与功能挂件**——恐龙视其为不存在，因此既不会去啃它，也不会被它挡路。次日现代人走回来可续建。
  10. **核心改名为"废弃船舱"**：见[故事背景](#故事背景)。界面与文档文案统一替换，内部 id `core`、HP 与 `game_lost` 判定全部不变——纯改名，零机制成本。
  11. **恐龙寻址维持单一路径**：来袭阶段恐龙沿固定路径直指核心（废弃船舱），沿用 v0.0 的路径点逻辑不变。随机目标寻址留待 v0.x，见远期构想。
  12. **现代人硬碰撞（不可穿墙）**：现代人启用物理层遮罩 `collision_mask = 3`（`1 | 2`：Layer 1 地面 + Layer 2 建筑与墙体），使用标准物理滑移与阻挡，现代人绝不穿墙穿建筑。
  13. **RTS 经典操作映射（集中于 Config.CONTROLS）**：默认配置**鼠标右键走位**、**鼠标左键就位建造/选择**、**ESC 取消建造选型**、**空格键暂停/继续**，提供自然流畅的操作手感。
  14. **现代人 A* 网格智能绕障与包围盒边缘施工**：
      - 现代人移动全面接入 `GridManager.find_path()`，自动计算避开已完工建筑的最优航路点序列（8 方向平滑寻路、防切角及视线平滑），右键点击移动或就位施工不再被墙体阻断卡死；
      - 施工距离判定采用地块 2D 包围盒边缘检测（`dist_to_box`），即使蓝图紧邻已完工建筑，站在开放侧也能稳定就位开工，彻底根除蓝图卡在 pending 的问题；
      - 蓝图自动队列与右键指派：建筑完工后现代人自动寻访场上未完工蓝图继续施工；右键点击未完工蓝图可直接命令前往施工。

- **巢穴设计（v0.1 只做外部，内部是黑盒）**：
  - **黑盒**：巢穴内部不可见、不可进入，玩家只能看到外壳与出入口。内部结构留待 v0.x 设计，v0.1 不做。
  - **守卫恐龙**：巢穴外常驻若干守卫恐龙，平时在巢穴周围 `post_radius` 内活动。
    - **部署阶段**：守卫有警戒半径 `aggro_radius`——现代人或建筑进入该半径，守卫脱离岗位前去攻击；
    - **脱战返岗**：目标被毁、目标离开、或守卫追出 `leash_radius` 之后，守卫返回巢穴附近岗位；
    - 这是"靠近巢穴有代价"的主要机制，与时间成本叠加，彻底封死"开局冲脸建塔"。
  - **来袭阶段**：恐龙从巢穴出来沿路径进攻建筑——沿用 v0.0 `WaveManager` + `Dino` 逻辑，不变。
  - **胜利条件不变**：巢穴 HP 归零 → `game_won`。

- **已定核心决策（定稿确认）**：
  - **现代人被击杀直接 Game Over**：现代人在部署阶段拥有生命值（`HERO.hp`）。若被守卫恐龙击杀，**立即触发游戏失败（`game_lost` / Game Over）**！高收益伴随极高风险，促使玩家走位施工时必须保持警惕。
  - **现代人可以徒手攻击巢穴本身**：初期现代人攻击力较低（`damage = 1.0`），而巢穴常驻守卫恐龙严密驻守，当现代人接近巢穴进入守卫警戒范围时守卫会主动发起攻击，因此守卫恐龙自然防御了开局肉身拆巢漏洞，玩家需先诱杀守卫或依托塔防步步推进。

- **验收标准**：
  - [x] 部署倒计时归零自动进入来袭，"提前结束部署"按钮可跳过剩余时间；
  - [x] 现代人拥有无限 AP，建造仅扣减木材并消耗部署时间，HUD 隐藏 AP 并更新按钮文案；
  - [x] 现代人具备碰撞体且检测建筑层，无法穿过木墙与建筑；
  - [x] 现代人具备 A* 绕障寻路能力，点击移动与就位施工丝滑绕行已完工建筑；
  - [x] 蓝图边缘施工判定稳定，紧邻墙体的蓝图可正常施工完工，支持多蓝图自动队列续建；
  - [x] 默认鼠标右键走位/指定施工，鼠标左键就位建造，支持空格暂停与 ESC 取消；
  - [x] 现代人在部署阶段生命值归零立即触发 `game_lost` 判定游戏失败；
  - [x] 现代人可攻击巢穴与守卫恐龙，守卫在警戒范围内对现代人主动追击，追出脱战距离后返岗；
  - [x] 暂停期间倒计时、移动、施工全部冻结，且来袭阶段战斗不受暂停系统影响；
  - [x] 部署末尾开工的建筑保留为半成品：不生效、来袭阶段不被恐龙攻击也不挡路、次日可续建；
  - [x] 来袭阶段现代人完全隐藏且不可被攻击，全程只有建筑参与防御；
  - [x] 恐龙引入工业级围攻槽位（Attack Slots）与雷诺兹转向算法，进攻姿态锁定免受排斥力冲击，彻底消除抽搐与单列长龙。

- **v0.1 明确不做**：战争迷雾、随机时机进攻、恐龙随机目标寻址、现代人属性成长与装备科技树、巢穴内部结构、来袭阶段操作现代人。

### 2. ★ 恐龙大规模集群多层排队、包围寻路与防穿模优化 (Priority 1 — 已在 v0.1 实现)
- **现状与痛点解决**：
  - 彻底重构了恐龙行军与包围逻辑：
  - **环形围攻槽位（Ring Attack Slots）**：建筑周围配置 8 个内圈槽位与 8 个外圈槽位，恐龙就近认领空余槽位并扇形散开包抄，彻底消除 10+ 只恐龙单列排队长龙与傻站现象；
  - **攻击态锁定与防抽搐（Anti-Jitter Lock）**：恐龙进入 `ATTACKING` 状态后速度归零，并完全免除恐龙间软排斥力的位置推搡，彻底根除进攻时的剧烈抽搐抖动；
  - **纯转向力群落推进（Reynolds Steering Flocking）**：行军期间绝不瞬移物理坐标，仅计算速度转向力，配合放宽的行军走廊（$\pm 2.0\text{m}$），群落行进自然流畅；
  - **攻破建筑后的无重叠平滑过渡（Smooth Post-Breach Transit）**：
    - 消除瞬移回廊 Snap：重构走廊边缘约束算法，彻底消除外侧恐龙因 `clamp` 瞬移 1.0m 撞在中心恐龙身上的严重缺陷，改为平滑转向力回归；
    - 响应式插槽释放：监听 `EventBus.building_destroyed`，建筑倒塌瞬间所有围攻恐龙立即清空槽位与目标，无缝切回行军；
  - **双重防穿模与硬位置松弛解算器（Anti-Penetration & Hard Relaxation Solver）**：
    - 运行每帧 2-Pass 硬位置约束投影（`_resolve_dino_overlaps`），对任何间距 $< 0.88\text{m}$ 的恐龙对施行瞬时物理位移分离，且静止攻击中的恐龙作为静态锚点，移动恐龙主动向外滑开；
    - 动态防追尾限速（Anti-Tailgating & Speed Throttling）：恐龙向前侦测扇区内的行军友军，保持安全纵向车距并侧向变道超车，彻底杜绝相撞或相互穿模。

---

## v0.2 迭代规划与 TODO 清单 (Roadmap)

> **范围说明**：v0.2 **只做下面 9 项，做好即算达成**（第 9 项是实机试玩后的收尾）。其余全部移入 [v0.x 远期构想](#v0x-远期构想不排期仅记录)，本版本不碰——包括昼夜循环与夜行生物、恐龙种类扩展、科技升级链、摄像机控制与受击特效。
>
> 第 1 项是规则级改动（推翻 v0.0/v0.1 的三阶段状态机），是本版本主线；第 2、3 项是核心玩法；第 4~7 项是交互与可读性；第 8 项是 i18n 地基（越早做越省事）。

### 1. ★★ 取消阶段制 + 随机来袭

- **设计动机**：
  - v0.1 的部署阶段实测是**空转的**：部署时长 90 秒，而第 2 天起每天只有约 4 木头的产出，可施工内容仅 2~6 秒，玩家超过 90% 的时间在等待或直接点"提前结束部署"。
  - 根因是**资源靠"等"就能到手**——只要等待免费且有产出，时间就永远不会稀缺，实时系统与自动经济互相抵消。
  - 取消阶段划分后，时间连续流动、来袭时机不可预期，时间才真正成为唯一货币。

- **已定设计规则**：
  1. **取消三阶段状态机**：删除 部署 / 来袭 / 产出 的阶段切换，游戏进入**单一连续实时状态**。取消"提前结束部署"按钮与部署倒计时。保留暂停（实时下更必要）与 game over 终态锁定。
  2. **生产改为连续产出**：生产建筑不再在结算时刻发放资源，改为**按秒持续产出**（详见第 3 项）。原"每天 N 个"的数值全部折算为"每秒 X 个"。
  3. **来袭时机随机**：恐龙来袭不再绑定阶段，改由 `WaveManager` 自有的随机间隔触发——在配置区间内随机，**不是固定周期**。
  4. **强度逐步上升，且上升本身带随机性**：整体强度随时间推进而提高，但每次来袭的规模与构成在曲线附近随机浮动，**不是严格递增的公式**，玩家无法靠背数值预测。
  5. **仇恨优先级：塔 > 其他建筑 > 现代人**（承接上一轮已定决策）。取消阶段后现代人不再有"来袭时隐藏"的保护，**任何时候都可能被攻击并杀死**，但在有塔可打时不是首要目标。
  6. **恐龙寻址仍维持单一路径**：本项只改来袭的**时机与强度**，不改目标选择。随机目标寻址仍留 v0.x。

- **为什么恐龙会优先打塔而不是人（世界观解释）**：
  - **威胁优先（Threat-based Aggro）**：塔在持续开火、持续造成伤害，动物本能地攻击**正在伤害自己的东西**——这是真实的动物行为，也是玩家一眼就能理解的规则。
  - **气味陌生**：现代人的气味是这个时代从未出现过的，恐龙的本能不把他识别为猎物；只要他不主动挑衅，就不会成为首要目标。
  - **派生规则 A（诱饵）**：现代人**一旦主动攻击**，威胁度上升到与塔相当，会把仇恨拉到自己身上。这让"当诱饵给塔争取时间"成为一个主动战术，而不是一条例外。
  - **派生规则 B（优先级是相对的，不是免疫）**：附近没有塔或建筑可打时，现代人就是第一目标。**因此远离防线去采集或照料，是真实的生命风险**——这与第 2、3 项直接咬合。

- **子任务拆分**：
  - **[TODO] T1.1 阶段制拆除**：`GameState` 的阶段枚举与切换逻辑收敛为单一运行态；移除部署倒计时与"提前结束部署"入口；`EventBus` 的 `phase_changed` / `produce_phase` 相关信号一并清理或重新定义。
  - **[TODO] T1.2 随机来袭调度**：`WaveManager` 从"监听 ATTACK 阶段"改为自有随机定时器；实现强度曲线 + 随机浮动。
  - **[TODO] T1.3 仇恨优先级**：`Dino` 目标选择实现"塔 > 其他建筑 > 现代人"的威胁排序；现代人攻击时提升自身威胁度；周围无建筑时以现代人为目标。
  - **[TODO] T1.4 测试迁移**：**阶段状态机是 v0.0/v0.1 测试覆盖最重的部分，取消阶段会使大量套件失效**，需整体迁移到连续时间语义。这是 v0.2 最大的一块工作量，必须保持全量 Headless 绿色。

- **Config 占位数值**（全部进 `Config.gd`，以下仅为初始占位）：
  ```gdscript
  const RAIDS := {
      "interval_min": 45.0,         # 两次来袭的最小间隔（秒）
      "interval_max": 90.0,         # 最大间隔——区间内随机，不是固定周期
      "first_raid_delay": 60.0,     # 开局宽限期
      "intensity_per_minute": 0.15, # 强度随时间上升的斜率
      "intensity_jitter": 0.3,      # 强度随机浮动幅度（±30%）
  }
  ```

- **待定决策（实现前必须定，文档不预设答案）**：
  - **空闲时段的节奏控制**：取消"提前结束部署"后，若出现无事可做的空档，玩家只能干等。是否需要加速（快进）按钮？还是靠第 3 项的照料机制保证"永远有事可做"来消除空档？
  - **现代人死亡 = 直接失败，在全实时 + 随机来袭下是否过于严苛**：随机偷袭造成的即死缺乏挽回空间。是否改为重伤 / 被击退 / 付出代价复活？
  - **来袭预警**：随机来袭若毫无征兆会显得不公平。预警形式（声音、动物感知、雷达等）与提前量尚未定义——注意**预警的核心作用是让玩家判断"还来不来得及跑回去"**，提前量太短则形同虚设。

### 2. ★★ 地图资源分布（木 / 石 / 水，各有储量上限）

- **设计动机**：v0.1 开局只有固定的 10 木，地图上没有任何资源获取途径，地图形同虚设。资源上图之后，"去哪里"才成为决策。

- **已定设计规则**：
  1. **v0.2 只做三种资源：木头、石头、水**。地图上按类型分布资源节点。
  2. **每个节点有储量上限**，被开采会减少、可以**枯竭**——因此玩家必须不断向外寻找新的资源点，基地不能永远固守一处。
  3. 资源节点是地图内容（摆点 + 数据），不引入新系统。

- **子任务拆分**：
  - **[TODO] T2.1 资源节点实体**：`ResourceNode`（类型、储量上限、当前储量、枯竭后的表现）。
  - **[TODO] T2.2 地图分布**：在 `Main.tscn` 的 Map 下按类型摆放节点。
  - **[TODO] T2.3 资源表对齐**：`Config.RESOURCES` 目前是 `["wood","stone","food"]`——`food` 从未有产出途径，`water` 尚未声明，需要对齐。

- **待定决策**：
  - **`food` 是去是留**：v0.2 的地图资源是木 / 石 / 水，没有食物。`food` 是从 `RESOURCES` 中移除，还是保留为暂不产出的占位？
  - **水的用途**：水是新资源，它消耗在哪里（建筑成本？照料消耗？）尚未定义。
  - **节点枯竭后**：原地消失、还是留下空壳可视化提示"这里采完了"？

### 3. ★★ 资源建筑需要照料（限时运转 + 按秒产出）

- **设计动机**：这是解决"**如何让玩家一直忙碌**"的核心机制（思路参考 *The King is Watching* 的注意力/凝视系统）。它同时回答了世界观上的疑问——现代人造的不是"有看不见的工人的小屋"，而是**需要人照料的机械**。

- **已定设计规则**：
  1. **初始资源建筑需要照料**：走过去照料一次，建筑获得一段**限时自主运转**（`tend_duration` 秒）。
  2. **时限到了就停产**，需要现代人再来照料一次才能继续。
  3. **产出按秒计算**：运转期间每秒增加固定数量的资源（如每秒 X 木头），不再是"每天结算一次"。

- **为什么是"照料 + 缓冲"而不是"持续半径内才产出"**：持续半径会退化成绕着建筑转圈的琐事——那是忙碌但不是决策。蓄能式让**缓冲时长成为拴住玩家的绳子**：给锯木场上料 → 趁这段时间去推一座塔或采集 → 时限快到时决定是回去续上还是先干别的。忙碌感的真正来源是**几个软截止时间同时在逼近**，而不是活多。

- **子任务拆分**：
  - **[TODO] T3.1 照料状态机**：建筑增加 `tend_timer` 与运转/停产状态；复用现有施工状态机实现"走过去照料"。
  - **[TODO] T3.2 按秒产出**：生产挂件从监听 `produce_phase` 改为按 delta 持续累加。
  - **[TODO] T3.3 状态可见性**：建筑的剩余运转时间必须能被玩家看到（见第 4 项的 Option 栏）。

- **Config 占位数值**：
  ```gdscript
  # 每种生产建筑在 BUILDINGS 中新增：
  #   "tend_duration": 40.0,        # 照料一次可自主运转的秒数——第二大平衡旋钮
  #   "tend_time": 2.0,             # 照料动作本身耗时（秒）
  #   "produces_per_sec": {"wood": 0.5},
  ```

- **待定决策**：
  - **"初始"资源建筑的含义**：是否存在后续升级让建筑不再需要照料（或延长 `tend_duration`）？若有，升级链需要定义。
  - **照料是否消耗资源**（如上料需要投入原料），还是只消耗时间？

### 4. ★ 选中单位 → 右下角 Option 栏

- **设计动机**：把信息与指令从"全部铺在屏幕上"改成"**选中谁就看谁**"，屏幕更干净，也是 RTS 的成熟范式（指令面板 / Command Card）。

- **已定设计规则**：
  1. **左键点击任意单位**（建筑、现代人、恐龙等）即选中它。
  2. **屏幕右下角出现 Option 栏**，显示：该单位的**状态**，以及它**可以做的事情**（指令按钮）。
  3. **不再把所有信息常驻在屏幕上**——未选中任何单位时 Option 栏为空或隐藏。

- **子任务拆分**：
  - **[TODO] T4.1 选中系统**：左键拾取单位、选中高亮、取消选中。
  - **[TODO] T4.2 Option 栏容器**：右下角面板，按选中对象类型渲染状态与指令。
  - **[TODO] T4.3 HUD 瘦身**：把现有常驻显示迁移进 Option 栏，只保留必须全局可见的信息（资源总量等）。

- **待定决策**：
  - **哪些信息必须全局常驻**（资源总量、船舱 HP 一般需要常驻），哪些收进 Option 栏。

### 5. ★ 建筑加文字标签

- **已定设计规则**：建筑不能只靠颜色区分，**每个建筑要显示名字**，让玩家一眼知道造的是什么。
- **[TODO] T5.1**：建筑上方或表面显示 `Config.BUILDINGS[type].name`（如"自动哨位""水力锯木场"）。
- **待定决策**：标签形式是 3D 空间中的 `Label3D`（随镜头缩放、会被遮挡）还是屏幕空间叠加。建议先用 `Label3D`，成本最低。

### 6. ★ 现代人也走同一个 Option 栏（大选项 → 小选项）

- **已定设计规则**：
  1. **点击现代人**，在与第 4 项相同的 Option 栏里选择他要做的事。
  2. **两级结构**：大选项 → 小选项。**初始的大选项就是"建造"**，点"建造"后展开可造的建筑列表。
  3. 建造流程不变（选建筑 → 点地面 → 走过去施工），只是入口从常驻建造菜单改为 Option 栏。

- **子任务拆分**：
  - **[TODO] T6.1 两级指令结构**：Option 栏支持"大选项展开小选项"与返回上一级。
  - **[TODO] T6.2 建造入口迁移**：现有建造菜单迁入"建造"大选项之下。

- **待定决策**：
  - **除"建造"之外的大选项**（采集、照料、攻击、移动等）是否也进这个结构，还是保持鼠标直接操作？

### 7. ★ 建筑血量与施工完成度显示在界面上

- **设计动机**：玩家需要**不用选中就能一眼看清**每栋建筑还剩多少血、蓝图盖到几成。这与第 4 项的 Option 栏是两个层次：Option 栏是选中后看**详细**状态，本项是不选中也能在建筑身上直接看到**关键**状态。

- **已定设计规则**：
  1. **建筑显示血量**：能看清当前还剩多少血（血条或数值）。
  2. **未完工建筑显示施工完成度**：能看清盖到了百分之几。
  3. 二者**互斥**——同一栋建筑在施工中显示完成度、完工后显示血量，因此实现上是**同一个控件的两种模式**，不是两套东西。
  4. 与第 5 项的文字标签同属建筑上方的信息层，**建议合并为一个小信息控件**（名字 + 血量/进度），避免叠三层 UI 互相打架。

- **子任务拆分**：
  - **[TODO] T7.1 建筑信息控件**：建筑上方的小控件，承载第 5 项的名字 + 本项的血量条 / 进度条。
  - **[TODO] T7.2 血量绑定**：监听建筑受伤与 HP 变化，实时刷新。
  - **[TODO] T7.3 施工进度绑定**：绑定 v0.1 已有的 `build_progress`，完工时自动切换为血量模式。

- **待定决策**：
  - **血条的显示策略**：始终显示（看得最清楚，但满血的建筑多起来会很吵），还是只在非满血时显示（画面干净，但无法一眼巡检整条防线）？建议先做"非满血时显示"，实测后再调。
  - **控件形式**：3D 空间中的控件（随镜头缩放、可能被遮挡）还是屏幕空间叠加。与第 5 项应保持一致。

### 8. ★ 中英文 Locale 配置（i18n 地基，英文为默认语言）

- **设计动机**：**现在做便宜，以后做昂贵。** 从这一版起每新增一句硬编码文案，都是将来要返工的债；而第 5、6、7 项正在大量新增界面文案，此时铺好 i18n 地基最省事。目标是**让"加一种语言"从此变成填表，而不是改代码**。

- **现状盘点**（硬编码中文的位置）：
  | 文件 | 出现次数 | 内容 |
  | :--- | :--- | :--- |
  | `scripts/autoload/Config.gd` | 24 | 建筑名、恐龙名等 `name` 字段 |
  | `scripts/ui/HUD.gd` | 15 | 界面文案、提示语 |
  | `scripts/core/Main.gd` | 3 | 少量提示 |

- **已定设计规则**：
  1. **使用 Godot 原生 i18n**（`TranslationServer` + `tr()` + 翻译表文件），不自造一套。
  2. **v0.2 支持两种 locale**：`en`（英文）与 `zh_CN`（简体中文）。
  3. **英文（`en`）是默认语言**：游戏首次启动以英文呈现；`en` 同时作为**回退语言**——任何 locale 缺失的键都回退到英文，不允许出现空白或裸键名。
  4. **中文必须完整可读**：`zh_CN` 要求**全量覆盖、无占位、无机翻腔**，与英文版同等质量。两种语言都是一等公民，不存在"先凑合"的那一个。
  5. **可在选项中选择**，切换后界面即时生效，不需要重启。
  6. **`Config` 中的 `name` 字段改为翻译键**（如 `BUILDING_TOWER_NAME`），显示时统一走 `tr()`。这样 `Config` 仍是唯一数值来源，而文案来源是翻译表。
  7. **硬规则：从此以后所有新增界面文案必须走 `tr()`，不允许硬编码字符串；且新增一个键时必须同时补齐 `en` 与 `zh_CN` 两列**，不允许只填一种语言后续再说。这条比补齐现有翻译更重要——它防止债务继续增长。

- **子任务拆分**：
  - **[TODO] T8.1 翻译表**：建立 `en` / `zh_CN` 两列的翻译表文件，注册进项目设置；`ProjectSettings` 中设定默认 locale 与回退 locale 均为 `en`。
  - **[TODO] T8.2 Config 文案外置**：`name` 字段改为翻译键，所有读取处改走 `tr()`。
  - **[TODO] T8.3 HUD / Main 文案外置**：上表中的 39 处硬编码文案全部迁入翻译表，并产出对应英文。
  - **[TODO] T8.4 最小选项入口**：目前项目**没有任何设置界面**，需要加一个最小入口（一个下拉框即可）用于切换语言。
  - **[TODO] T8.5 语言偏好持久化**：记住玩家选择（一个轻量的用户配置文件，与"不做存档"的约定不冲突——存的是设置，不是进度）。
  - **[TODO] T8.6 完整性校验**：加一条自动化检查，断言 `en` 与 `zh_CN` 的键集合完全一致、且无空值——把"两种语言都完整"变成测试保障的事，而不是靠人记得。

- **与其他项的关系**：
  - 第 5 项（建筑文字标签）与第 7 项（血量/进度控件）的文案必须一开始就走 `tr()`，否则刚做完就要返工；
  - 第 4、6 项 Option 栏的所有指令名称同理；
  - 本项建立的选项入口，正是 v0.x"玩家自定义键位与控制设置面板"未来要扩展的地方。

- **验收标准**：
  - [ ] 首次启动默认显示英文；
  - [ ] 选项中可切换 `en` / `zh_CN`，即时生效且重启后保持上次选择；
  - [ ] 两种语言键集合完全一致、无空值、无裸键名泄漏到界面（由 T8.6 自动化校验保障）；
  - [ ] 中文文案完整可读，无占位文本；
  - [ ] 项目中已无硬编码界面字符串。

### 9. ★ 追加交付（v0.2 收尾）

这一批是 v0.2 主体交付后根据实机试玩补的，已全部完成。

- **[x] 移除顶栏 legacy 建造按钮**：三个 per-building 建造按钮（`BuildTowerBtn` / `BuildWallBtn` / `BuildLumberHutBtn`）连同它们的点击处理与文案更新逻辑一并删除。建造现在**只有一个入口**：选中现代人 → Option 栏 → 建造。相关测试断言已迁移到 Option 栏。
- **[x] 建造菜单改为 Config 驱动**：新增 `Config.BUILDABLE_TYPES`，取代 OptionPanel 里硬编码的 `["wall","tower","lumber_hut"]`。采石场与取水站此前已注册进 `BuildSystem` 但**任何 UI 都点不到**，现在可以建造了。
- **[x] 阶段时代控件隐藏**：AP 标签、部署倒计时、阶段名、"提前结束部署"按钮在 v0.2 取消阶段后已无对应操作，从玩家视野中移除（节点保留以便测试，待 `GameState` 的阶段枚举真正删除后再一并清理，见 v0.x）。
- **[x] 字体与缩放全部 Config 驱动**：新增 `Config.UI` 段，统一管理 HUD / Option 栏 / 世界空间 3D 文字的字号与 Option 栏尺寸。项目以 1280×720 设计视口 + `canvas_items` 拉伸全屏运行，因此 UI 在 1080p 自动放大 1.5×、4K 放大 3×，**界面随分辨率缩放由引擎负责**。建筑与资源点头顶的 `Label3D` 改用 `fixed_size = true`，屏幕尺寸恒定、不随镜头远近缩放。
- **[x] 机械必须真的采集**：**所有**生产建筑（不再只有伐木屋）在运转时会在 `harvest_range` 内寻找最近的、未枯竭的、类型匹配的资源点，并**从该资源点的储量里扣除**每一个入库单位。范围内没有可采资源 → 产出为零，不再凭空造资源。原先"世界上没有树就免费产出"的兜底已移除。
  - 逻辑上提到 `ProducerBuilding` 基类，`LumberHut` 收敛为只提供伐木专属文案；采石场与取水站因此自动获得同一套行为。
  - 采集半径可视化（选中建筑时显示地面圆环）也上提到基类，圆环颜色取自该资源在 `Config.RESOURCE_NODES` 里的颜色。
  - **配套调参**：资源点容量相应上调（木 30→150、石 20→100、水 40→120）。接上真实资源点后，原容量会在一次半照料内被抽干。
- **[x] HUD 显示全部资源**：此前只显示木材，石头与水虽然能产出但玩家看不到。现已补上 `StoneLabel` / `WaterLabel`。
- **[x] 场景内硬编码中文清理**：`HUD.tscn` 的文本默认值（提示、部署、暂停、结算文案）此前仍是中文，而默认语言是英文——玩家在 tr() 生效前会看到一帧中文。已全部改为英文默认值，并移除场景里逐节点的 `font_size` 覆写（改由 `Config.UI` 统一管理）。
- **[x] 资源钱包兜底修正**：`GameState` 中三处硬编码的 `{"wood","stone","food"}` 兜底字典**缺少 `water`**，而 `add_resources()` 会 `continue` 掉不在字典里的资源——水会被静默丢弃。改为从 `Config.RESOURCES` / `INITIAL_RESOURCES` 派生。

- **[x] 建造预览（鼠标悬浮虚影）**：选中建筑类型后，一个半透明虚影跟随鼠标吸附到格子上，**绿色=可放置 / 红色=不可放置**；带作用范围的建筑（塔的射程、生产建筑的采集半径）同时画出覆盖圆环，圆环内的资源点**高亮发光**，一眼就能看出这个位置能不能采到东西。
- **[x] 已建建筑也显示覆盖范围**：范围圆环从 `ProducerBuilding` 上提到 `Building` 基类，子类只需回答"半径多大、什么颜色"。因此**防御塔现在也会在被选中时显示射程**，木墙这类没有作用范围的建筑则不画环。
- **[x] 修复"恐龙进攻时无法建造"**：`GameState._process` 里的部署倒计时仍在运行，90 秒后自动 `advance_phase()` 切到 ATTACK，建造被拦并弹出"非部署阶段，无法建造"。v0.2 第 1 项的"取消阶段制"此前只绕过了英雄与波次管理器，**没有绕过 GameState 自己**。现在 `GameState.continuous_mode` 打开后倒计时不再运行、阶段固定在 DEPLOY，所有遗留的 `current_phase` 判断自动失效；`reset_game()` 会复位该标志（GameState 是 autoload，否则会污染后续测试与重开）。
- **[x] 经济平衡重做**：原伐木屋成本 3 木、每次照料产出 20 木（**6.7 倍回报**），木材爆炸且建筑随便造。新数值见 `Config.BUILDINGS` 上方的注释块：伐木屋 12 木 → 每次照料 10 木（约 1.2 次回本），哨位 20 木（约两次照料），木墙 5 木；手工采集降到 0.5 木/秒，明显劣于机械（机械运转时人可以去做别的）。开局 10 木不够建伐木屋，**第一步必然是出门采集**。

**测试层面的连带修复**：这次调平衡打破了 318 条断言，因为大量测试把 v0.0 时代的数值（初始 10 木、墙 2 木、塔 4 木、伐木屋每次 2 木）直接写死。已全部改为**从 Config 派生**（`test_base.cost_of()` / 各套件的 `SEED_WOOD` 与产出查询），今后再调平衡不会重演。

- **[x] Option 栏的选中切换逻辑**：此前选中只在左键点击时改变，面板不会跟随现代人正在做的事。现在按 RTS 惯例处理：
  - **默认跟随当前任务**——现代人在建造 / 采集 / 照料时，面板自动显示他正在处理的那个目标，玩家不用点就能看进度；
  - **任务结束自动回到人身上**——不再停留在刚造完的东西上；
  - **玩家点击即"钉住"**——手动选中的单位不会被自动跟随抢走（正在看一座塔时不该因为现代人修完一堵墙就被拽走，这是这类 UI 最典型的失败方式）；
  - **点击现代人或点击空地释放钉住**，自动跟随恢复；
  - **被钉住的单位消失**（拆毁、枯竭）时释放钉住并回到现代人。

- **[x] 木栅栏（原木墙）改为 1 木一根，可连排铺设**：插在土里的尖木头，每根 1 木。建造模式在**放置后不会退出**，虚影一直跟着鼠标，玩家可以连点铺出一整排待建的栅栏，按 ESC 退出；现代人随后**一根一根地把它们立起来**（连续放置与蓝图排队的机制 v0.0/v0.1 已有，这次只是让价格便宜到值得这么用）。
  - **配套调整**：血量 30 → 8。1 木换 30 血会让栅栏变成性价比碾压一切的选择，等于废掉刚调好的经济。现在每根约 8 血/1 木，铺一排十根 ≈ 80 血/10 木，和改动前两堵墙的性价比相当。
- **[x] 建造时间由价格推导**：新增 `Config.get_build_time()`，`build_time = max(下限, 总成本 × 每单位秒数)`。各建筑不再各自声明 `build_time`（有测试断言这一点，防止两个数字漂移）。设计者只调**一个**数字（价格），时间自动跟随：栅栏 1 木 → 0.5 秒（下限），伐木屋 12 木 → 4.8 秒，哨位 20 木 → 8 秒。
- **[x] 右键只对"当前选中的对象"下令**：此前无论选中什么，右键一律指挥现代人走动。现在只有**现代人处于选中状态时**右键才发号施令；玩家点开一座塔查看时，右键不再兼职"走过去"——这是 RTS 的通行约定。点空地或点现代人即可交回指挥权。
- **[x] ESC 菜单**：ESC 现在**逐层剥离**——先取消建造虚影，再取消钉住的选中，都没有时才打开菜单（菜单打开时再按 ESC 关闭）。菜单三项：**继续游戏 / 游戏设置 / 退出游戏**，设置页内含**语言切换**（en / zh_CN，即时生效）。
  - 打开菜单会暂停世界，关闭时**只解除菜单自己加的那次暂停**——玩家自己按空格暂停过的话，关菜单不会替他恢复。
  - 这同时补上了 v0.2 第 8 项的 `T8.4 最小选项入口`，v0.x 的"自定义键位设置面板"以后可以直接扩展这个菜单。

- **[x] 建造菜单改为"一眼可读"，而不是逐条读数字**：此前每个按钮都写着 `名字 (N木)`，玩家得逐个拿数字和自己的存量比对。改成正规游戏的通行做法：
  - **按钮只放名字**；
  - **成本与建造时间放在下方的详情行**，鼠标悬停哪一条就显示哪一条；
  - **买不起的条目直接置灰禁用**——能不能造是"看"出来的，不是"算"出来的；
  - 买不起时详情行显示「需要 N 木（现有 M）」并标红，直接告诉你差多少。
  - （图标化留待有美术之后，文字版布局已经就位。）
- **[x] 连续建造按点击顺序施工**：`Building` 记录下笔顺序，现代人改为**取最早排下的蓝图**，而不是离自己最近的。铺一排栅栏时会按你点的顺序一根根立起来——"就近"从玩家视角看是随机的，因为他根本没在想现代人站在哪。
- **[x] ESC 菜单居中**：改用铺满屏幕的 `CenterContainer` 承载面板（只靠 anchor 在面板尺寸后于布局确定时会被顶到角落），并加了一层半透明遮罩，让菜单读起来是模态层而不是浮在角落的一个框。

- **[x] ESC 菜单真正居中（上一轮没修好）**：上一轮换成 `CenterContainer` 后仍在左上角。真实原因是 **PauseMenu 自身是 0×0**——`set_anchors_preset()` 的语义是"重算 offsets 以**保持当前的位置和尺寸**"，从 0×0 出发就永远是 0×0，里面的一切自然都堆在角上。改用 `set_anchors_and_offsets_preset()` 后面板中心与视口中心精确重合。对应的测试也从"面板的父节点是不是 CenterContainer"（结构对但位置错也能通过）改成**实测坐标**。
- **[x] 连续建造真正按点击顺序（上一轮没修好）**：上一轮给蓝图加了下笔序号，但现代人仍会被后点的抢走。真实原因在 `order_build()` 的守卫**只拦 `BUILDING` 状态**——他还在**走向**第一根的路上（`MOVING`）时，每一次新点击都会改掉他的目标，于是一排栅栏是倒着建的。守卫扩展到"手上已有未完工蓝图"即不改派。
- **[x] 建筑之间留出天然通道，防止把人封死**：建筑原本占满格子的 1.8/2.0，只剩 0.2 的缝，而人宽 0.8——一圈建筑就能把他永久关在里面。新增 `Config.get_building_footprint()`，由**格子尺寸 − 人的宽度 − 余量**推导（2.0 − 0.8 − 0.2 = 1.0），相邻建筑之间恒有 1.0m 通道，比人宽 0.2m。人的碰撞宽度也改为读 `Config.HERO.width`，两处不再各写各的。
- **[x] 修复"对着树采集后人就动不了"**：这是上一轮"右键只对选中对象下令"的回归——点树会把面板钉在树上，右键随之失效。规则收紧为：**只有玩家自己的建筑**才会夺走指挥权（它们有自己的指令），**树木岩石这类景物是纯信息展示**，选中它们不影响操作现代人。
- **[x] 移除资源点的"采集"按钮**：选中人对着树右键就能采集，这更直觉；按钮只是同一件事的第二条更慢的路径。

- **[x] 左右键职责彻底分开（取代之前的"跟随/钉住"模型）**：
  - **右键 = 操作。** 现代人对光标下的目标做它所支持的事——采树、续建蓝图、照料机械、攻击恐龙，什么都不是就走过去。**右键永远不改变状态栏显示的内容。**
  - **左键 = 选中查看。** 只改变状态栏，不产生任何操作。点空地回到现代人。
  - 因此对树右键是直接采集、**不显示树的状态**；要看树的状态就对它左键。
  - **删掉了之前的三层复杂度**：状态栏自动跟随现代人当前任务、手动选中的"钉住"标志、以及"选中建筑时右键失效"的门控。它们是为多单位 RTS 设计的约定，在只有一个化身的游戏里只会互相打架——那条门控正是"点了树之后人就动不了"的直接原因。
  - 规则简化后：状态栏显示的永远是**你最后一次左键点的东西**，默认是现代人；显示对象消失时回到现代人。

- **[x] 修复"照料/采集途中下别的命令,人被拽回去"**:复现路径是「左键机器 → 点照料 → 点人 → 右键机器」之后人不听指挥。
  - 根因:现代人身上有四个目标字段,而**五个指令函数各自手写一遍清空列表**,其中 `move_to()` 只清了两个,漏掉了"照料目标"和"采集目标"。于是照料/采集尚未完成时下移动命令,`_process_moving` 看到残留目标又把他拉回机器,他看起来就像失去响应了。
  - 上一轮那条"采完树人就动不了"其实有**两个**原因,我当时只修了选中门控那一个,这才是另一半。
  - 改法不是补上漏掉的两行,而是抽出唯一的 `_clear_orders()`,五个指令共用——这个 bug 存在的原因就是同一份列表被抄了五遍。

- **[x] 移除现代人界面的 Stop 按钮**：操作已全部收束在右键直觉指令（移动、攻击、采集、建造、维护），一级菜单不再需要冗余的 `[ Stop ]` 按钮，界面只保留清晰的 `[ Build ]`（底层 `Hero.order_stop()` API 仍保留供脚本和单元测试调用）。
- **[x] 建筑高亮过滤标准化（只高亮产生交互的目标）**：
  - **规则标准化**：建筑在 `Config.BUILDINGS` 中声明 `interacts_with_resources`（如 `lumber_hut` 对应 `["wood"]`，`quarry` 对应 `["stone"]`，`hunting_hut` 对应 `["water"]`），或由 `produces_per_sec` 自动推导；炮塔与墙体无资源交互返回空。
  - **实体层通用接口**：`Building` 提供 `get_interactable_resource_types()` 与 `can_interact_with(target)`，退出场景或取消选中时清理高亮。
  - **Review 后修正（高亮归属）**：原实现给每座建筑各自一份高亮列表，而 `unit_selected` 是**扇出**给所有建筑的、顺序不确定。于是「选中 A → 选中 B → 再选回 A」时，A 先点亮共享的树、B 后收到"取消选中"再清掉它那份**过期列表**，把树一起灭了。改为**全局唯一一份高亮列表 + 归属者**：只有点亮它的那座建筑才能熄灭它，与信号到达顺序无关。
  - **Review 后修正（同一规则写了三遍）**：「这个资源点还能不能用」原本在 `Building.can_interact_with`、`Main` 的建造预览、以及 `ProducerBuilding` 的推导里各写一遍。收敛为 `ResourceNode.is_available()`，三处共用；`ProducerBuilding` 中与 `Config` 完全重复的那份推导已删除。
  - **预览与选中双向对齐**：在 `Main` 建造预览以及场景中左键选中建筑时，伐木场只高亮范围内的活树（忽略石头、水和已采空的枯桩），炮塔与围墙只显示射程圈不高亮任何资源点，杜绝杂乱与歧义。

- **[x] 占地尺寸改为每个建筑各自的属性，墙成为真正的屏障**：
  - 原先占地是一个**全局推导值**（格子 − 人宽 − 余量 = 1.0），所有建筑一律留出通道。现在 `footprint` 是 `Config.BUILDINGS` 里的**可选属性**：声明了就用自己的，没声明就退回那个"留出通道"的默认值。
  - **木栅栏声明 `footprint = 1.9`**（格子 2.0），相邻两根之间只剩 0.1m 缝——**一排栅栏真正合拢成一道围栏**，看起来像围栏，也确实拦得住东西（为 v0.x 的恐龙寻路绕行做好了准备：会绕路的恐龙将无法从缝里穿过）。
  - 新增 `Config.is_barrier_building(type_id)`，由"相邻同类之间的缝是否窄于人宽"判定，不需要额外再声明一个布尔。
  - **被围死的出口是拆除**：左键选中相邻的任意建筑 → 拆除，格子随即释放。拆除不要求现代人站在旁边，所以被自己围在中间时永远有解。
  - **副作用（预期内）**：栅栏挨着任何工坊时缝为 0.55m，也过不去人——一根栅栏顶住建筑同样算封闭。
  - 工坊类（哨位/伐木屋/采石场/取水站）保持 1.0 占地、1.0m 通道，**不会意外把人关住**。

**遗留待你决定的一项**：`food` 仍在 `Config.RESOURCES` 与 `INITIAL_RESOURCES` 中，但没有生产建筑、没有资源点、HUD 也不显示它——是一列恒为 0 的死数据。是从资源表移除，还是保留为占位？（涉及 15 个测试文件里的引用，所以没有自行决定。）同时 `hunting_hut`（猎屋 / Hunting Hut）现在产出的是水，名字与行为不符，建议改名为取水站一类。

## v0.3 迭代规划与 TODO 清单

> 本节是 v0.2 实机试玩后定下的方向。三项已全部交付。

### 1. ★★ 资源统一为掉落物，全部需要人去捡 — **已完成**

- **设计动机**：目前资源有两条互不相干的获取途径——手工采集是"走过去按住"，生产建筑是"数字自己涨"。统一成掉落之后，**所有资源都经由现代人的身体进入仓库**，"一个身体撑一个基地"这个核心设定才真正贯穿到经济层。

- **已定设计规则**：
  1. **恐龙被击杀掉落恐龙肉**；
  2. **伐木厂产出的是地上的木头堆，不是直接入库的数字**；
  3. **所有掉落物都必须玩家走过去捡**才计入仓库；
  4. **开局在船舱附近预先放置一批初始资源**，替代现在直接塞进仓库的 `INITIAL_RESOURCES`——开局第一件事就是去捡，机制从第一秒就自洽。

**交付内容**：

- **[x] `DropItem`：地上的一堆东西**。三个关键约束决定了它的形态：**不占格子、没有碰撞体**（一堆木头绝不能挡建造、不能顶开寻路）；**不可选中**（走过去就捡，否则只是把“点树”换成了“点地上的东西”，操作量反而变多）；**同类自动并堆**并显示数量（一场大战不至于把地图铺满单位数 1）。
- **[x] 自动拾取**：现代人每一个物理帧都会扫一遍身边（`pickup_radius`），**不论他当时在做什么**——去建造的路上路过一堆木头也会顺手拿走。
- **[x] 四个产出源全部改为掉落**：恐龙死亡掉肉（`Config.DINOS[type].drops`）；生产建筑把产出堆在自己脚边（资源点储量的扣减逻辑不变——机器仍然真的从树上采，只是不帮你搬回家）；手工采集也先落地（人就站在上面，下一帧自己捡起来，**手感不变但规则统一了**）；拆除退回的材料变成废墟里的木头。
- **[x] 开局物资改为撒在船舱四周**：`INITIAL_RESOURCES` 全部归零，`Config.DROPS.opening_stock` 按环撒在船舱周围。环半径必须让**每一堆都落在现代人拾取半径之外**——否则第一帧就自动入账，这一课就白上了。
- **[x] 反馈**：拾取时原地飘一个 `+N`（`Fx.floating_text`，用该资源的颜色）、一个拾取音效、掉落物飞向现代人的补间。HUD 新增**肉**的读数——没有读数的资源，玩家看不到自己赚到了。

**原先的四个待定问题，结论全部是 `Config.DROPS` 里的一个数，而不是代码里的一个形状**：

1. **掉落物不会过期**（`lifetime: 0`）。否则在地图边上打赢的一波就等于白打；地面整洁靠**并堆**而不是靠计时器。想让捡东西变成有时限的任务，把它调成 > 0 即可。
2. **同类并堆**（`merge_radius`），堆上显示数量。
3. **上限限的是“堆”的个数，不是资源**（`max_on_ground`）：到上限时新掉落并入最近的同类堆，**玩家赚到的东西永远不会被删掉**。
4. **战后去战场上捡东西是选择而不是截止时间**——因为不过期。这是刻意选的温和答法，等实机试玩过再谈。

**恐龙肉（`food`）目前只能收集，还没有消耗途径**。它从一列没有产出途径的死数据变成了真的会入账的东西，**花在哪里属于本文末尾 v0.x 的“人的升级系统”**。

**仍然直接入账的一条路径**：`ProducerBuilding._on_produce_phase()`——旧阶段制的一次性结算。它在实际游戏里跑不到（连续模式把阶段固定在 DEPLOY），故意保持原样，让 v0.0 时代的压力套件继续覆盖那条已退役的机制；函数头上写清了这两件事。

### 2. ★ 打击反馈与选中反馈（让它像个游戏）— **已完成**

目前 `Building._on_damaged()` 是空函数，恐龙死亡是直接 `queue_free()` 瞬间消失，整个项目没有任何音频。塔有一条黄色曳光线，除此之外没有任何东西会对玩家的操作做出反应——这是"看上去不像游戏"的主因，与方块占位美术无关。

- **[TODO] 受击闪白**：建筑与恐龙挨打时短暂变白。
- **[TODO] 死亡碎块**：恐龙死亡时炸开几个小方块，而不是凭空消失。
- **[TODO] 命中音效**：开火、命中、死亡、建造完成。
- **[TODO] 血条**：战斗中可见的血量条（与 v0.2 第 7 项的 3D 信息控件合并，不要再叠一层）。
- **[TODO] 选中圈**：被选中的单位脚下画一个环绕的圈，和 v0.2 已有的覆盖范围圆环区分开（选中圈跟随单位、恒定大小；覆盖圈是作用范围、按半径画）。

**交付内容**：

- **[x] 受击闪白**：建筑、恐龙、现代人挨打时短暂变白。实现上闪白作用在材质的**副本**上，绝不会把白色烙进建筑自己的颜色或高亮状态里。
- **[x] 死亡碎块**：恐龙与建筑倒下时抛出若干小方块，带旋转、下落与淡出，而不是凭空消失。碎块挂在**当前场景**之下，所以重开一局会连它们一起带走。
- **[x] 音效**：命中、死亡、建造完成、来袭预警。**音频在启动时用代码合成**（`AudioStreamWAV`），仓库里不引入任何二进制资源，也不依赖 Godot 的导入步骤（headless 测试环境里导入不会跑）。8 条声部轮询播放，避免互相打断。
- **[x] 血条 / 施工进度条**：`StatusBar3D` 挂在与名字标签同一层。未完工时显示施工进度（琥珀色），完工后显示血量（低于 35% 转红）；**满血时隐藏**，避免画面到处是条。
- **[x] 选中圈**：`SelectionRing3D`，脚下一个细环，**尺寸固定**。与"作用范围圈"刻意区分——选中圈说"你点的是这个"，范围圈说"它影响这一片"；两者同尺寸或同颜色会让其中一个失去意义。现代人也加入了可选中组并拥有选中圈。
- **[x] 全部数值集中在 `Config.FEEDBACK`**，含一个 `audio_enabled` 总开关。

**设计约束**：反馈层绝不能反过来影响模拟，所以 `Fx` 的每个入口都能在**无渲染、无音频设备**的 headless 环境下安全调用（传 null、传游离节点、传未知音效 id 都有测试覆盖）。

> **注**：本节属于"反馈层"，不是美术。模型、贴图、光照、地形等美术工作仍然推迟——设计还在动，现在做的会扔。反馈层则是玩法的一部分，且不依赖任何未定的设计决策，可以随时插入。

### 3. ★ 实机试玩后的三项调整（建造时间 / 体量 / 栅栏伤害）— **已完成**

玩家提出的三条：**建造时间需要再长一点，凸显越贵的越久**；**木栅栏太大、防御塔太小，size 应该反过来**；**基础的木栅栏应该就有一定的伤害，恐龙要过需要受到伤害**。

- **[x] 建造时间改为超线性曲线**：`get_build_time()` 从总造价推导，`pow(造价, BUILD_TIME_EXPONENT) * BUILD_SECONDS_PER_RESOURCE`，指数 > 1。所以**价格翻倍，等待不止翻倍**，贵的东西真正压手。栅栏瞬建，作坊与防御塔十几秒，采石场最久；开局“作坊 → 照料 → 防御塔”仍能卡进首波宽限内。建筑不得自带 `build_time`，由测试强制（否则两个数字会分家）。
- **[x] 体量对调**：新增每建筑的 `height` 与 `mesh_style`。**栅栏宽而矮**（占满格子、低于普通建筑，画成一排尖木桩而不是一块方块），**防御塔窄而高**（旁边留出比现代人更宽的通道，所以围一圈防御塔不会把人困死）。名字标签与血条的高度改为**跟随建筑自身高度**，不再是写死的值。
- **[x] 栅栏接触伤害**：`Wall` 按固定节拍（`contact_tick`）对接触距离（`contact_range`）内的恐龙造成伤害，而不是每帧——否则机器越快栅栏越强。接触距离必须**能够到恐龙实际站位的攻击内圈**（`DINO_ATTACK_SLOT_RADIUS_INNER`），否则启了伤害也打不到人。
  - **平衡意图：削血而不是击杀**。迅猛龙啃穿一根栅栏所需的时间里，会被栅栏削掉大半血但活着出来，**收尾交给防御塔或现代人**；大型兽脚类基本不在意。防御塔的 DPS 仍高于栅栏，否则没人需要防御塔了。以上关系都由测试从 `Config` 推导，不重写数值。
  - **明码标价**：建造菜单的详情行会多出一段伤害数值，选中栅栏时 Option 栏也会报出来——玩家在**花木头之前**就知道它会咬人，而不是靠看尸体推测。

---

## v0.4 迭代规划与 TODO 清单 — **已完成**

> 本节是 v0.3 实机试玩后的一次设计转向。五项已全部交付。

**设计动机**：照料机制让**建筑成了工人、人成了维修工**，这跟"一个身体撑起一个基地"是反的。生产建筑本质上是"用木头买时间"的机器，它恰好削弱了这个游戏最核心的张力——每一秒采集都是没在建防御的一秒。

**转向后的两句话**：**建筑只负责防御；船舱是工作室。**

### 1. ★★ 删掉照料机制与全部采集建筑

- 删除 `lumber_hut` / `quarry` / `hunting_hut`，以及 `ProducerBuilding.gd`、`LumberHut.gd` 和整套照料（`order_tend`、`tend()`、运转计时、覆盖圈、"范围内无可用树木"状态）。
- **木头只能人手采**。采集速度成为后续升级人的方向之一（见第 2 条的工作台）。
- 建造菜单只剩**木栅栏**和**防御塔**（塔还需要解锁，见第 3 条）——"建筑是防御"变成界面上看得见的事实。
- ⚠️ **540 个测试函数里有约 100 个碰到 producer/tending**。其中相当一部分只是拿 `lumber_hut` 当"一个方便的建筑"用，一行换成塔即可；真正要删的是 v0.2 第 3 项那一整块。

### 2. ★★ 船舱 = 小工作室，两个工位

一句话规则：**材料造工具，食物给灵感。**

- **工作台**：龙骨 + 木头 + 石头（未来还能加各种材料）→ 解锁**能力**（我能做什么动作：能采石、采得更快）。
- **厨房**：肉 → 做料理 → **好的料理让人领悟新的蓝图** → 解锁**建筑**（我能造什么）。
- **不需要背包，解锁就能用**。人身上不加属性栏、不加格子、不加库存——这是不变成 RPG 的底线。
- 复用已有机制：制作进度 = 施工进度（同一条进度条）；点船舱 → 右下角 Option 栏列出能做的东西（两级菜单已有）。
- 这一条同时落实了两条早就定下的硬规矩：**升级路径必须依赖建造**（工具产自你守的那栋建筑），**人和塔的作用要分清楚**（工作台给能力，厨房给建筑）。

### 3. ★★ 石头链：石镐要龙骨，所以必须先杀一只恐龙

"攒木头就能造工具"太简单，因为那是**同一个动词的更大数字**。门槛应该是一个你还没做过的动作。

- 恐龙尸体给**肉 + 骨**（掉落框架本来就支持一个 `drops` 掉多样，不是新机制）。
- **石镐**（木 + 骨，在船舱工作台做）→ 才能采石。
- **石头 → 塔**，落实 v0.x 记的"防御塔这种强大的东西不应该木头到了就能造"。
- 完整链条：

  ```
  采木 → 栅栏（削血 0.3/s）→ 你补刀 → 掉骨头 → 船舱做镐 → 采石 → 造塔
  ```

- **这条把第一波来袭从纯粹的威胁变成了"你需要它"**。塔防游戏里波次永远只是"挺过去"的东西，在这里它同时是补给线。栅栏也第一次有了"帮你拿到第一滴血"的作用，而不只是拖延。

### 4. ★ 数值重调

- 现有的造价、建造时间、来袭间隔全部是围绕"作坊 → 照料 → 塔"调的，没有作坊之后开局变成"手采 → 栅栏 → 打一只 → 镐 → 石 → 塔"，要整体重算首波宽限期。
- ⚠️ **硬约束**：第 3 条把"第一次战斗"变成了必经之路，所以**首波必须能在栅栏后面打赢**。今天的数值刚好够（2 只迅猛龙，各 3 血 / 1 伤害；人 10 血 / 1 伤害每秒 / 攻击距离 2m，硬碰硬掉 8~9 血，站栅栏后面就舒服）。这条一旦定下来，**首波只数和恐龙强度就从平衡旋钮变成了硬约束**，不能再随手上调。

### 5. ★ 地形阻挡区（丘陵）

**规则**：地图上有**真实高度的小丘陵**，不能在上面建造，也不能走上去——当作**不可进入的区域**。

**为什么放在 v0.4 而不是 v0.5**：这是玩法规则，不是外观。它改变关卡布局、寻路和建造合法性，而 v0.4 本来就要整体重调数值；留到 v0.5 等于先在一块空地上把难度调好，再加隘口，然后全部重调一遍。**外观（真的像丘陵的模型与高度）留在 v0.5，v0.4 先用占位方块**——这就是项目自己的"先丑后美"。

**实现上是一处很小的改动**，因为现有代码只有两个关口：

- `GridManager.is_cell_walkable()` —— 人的寻路只从这里过；
- `BuildSystem.can_place_building()` —— 建造合法性只从这里过。

加一个**静态的 `blocked_cells`**（区别于动态的 `occupied_cells`：前者是地形，永远不变；后者是建筑，会来会走），两处各加一行判断，格子来源写进 `Config.MAP`。

**已拍板：丘陵也挡恐龙。** 所以本条同时包含一件不小的改动——**把恐龙从走廊移到格子寻路上**。

现状是：恐龙沿 `waypoints` 走廊前进（`Dino.advance_towards_waypoint()` + `_clamp_to_corridor()`），**完全不看 `is_cell_walkable()`**。只挡玩家的话，丘陵就只是一条单方面的限制；挡了恐龙，地形才会决定它们**从哪来**，于是栅栏摆在哪里、塔摆在哪里才第一次有了答案——这是隘口成立的前提。

代价要认下来：这会牵动**集群分散**（`_calculate_steering_separation`）与**攻击站位**（attack slot，围绕建筑的 16 个位置）两套已有机制，它们都假设恐龙在开阔地上自由移动。实现时按这个顺序走，每步保持全量测试绿：

1. 恐龙的目标点仍是 `waypoints`，但**两点之间改用 `GridManager.find_path()`**，走廊钳制（`_clamp_to_corridor`）退役；
2. 分散力只在**可通行格子**内推开，不能把恐龙推进山里；
3. 攻击站位落在山里的要被跳过，否则会出现"站在山上啃建筑"。

### 交付内容（v0.4 全部五项已完成）

**1. 删掉照料机制与全部采集建筑** — `lumber_hut` / `quarry` / `hunting_hut`、`ProducerBuilding.gd`、`LumberHut.gd`、整套照料路径（`order_tend`、`State.TENDING`、运转计时、`building_tended`、Tend 按钮）全部删除，连同"选中建筑高亮它覆盖的资源点"——那个功能唯一的主语是机器。**45 个测试函数是删掉的而不是修好的**：主语没了，留着它们只是在断言死代码还是死的。

**2. 船舱 = 工作室** — `scenes/CabinInterior.tscn` 是独立场景文件，但**实例化进关卡、停在地图下方 200 米**，进入只是切相机。**绝不是 `change_scene`**：换场景会卸载外面的世界，"回船舱"就不再有代价，这条设计的支点就塌了。三条配套规则：工作只在人真的站在屋里时推进；ESC 瞬间出去（进去得是风险，不能是陷阱）；右键船舱 = 走过去 + 到了自动进。

**3. 石头链** — 两道门都是**数据声明**（`RESOURCE_NODES.stone.requires_unlock` / `BUILDINGS.tower.requires_unlock`），不是代码里的分支。塔现在是 **木 8 + 石 4 + 蓝图**。

**4. 数值重调** — 木栅栏造价 1 → 2（1 的时候修一根和重建一根同价，修理在最便宜的东西上失去意义）；烤肉 3 肉 → 2 肉（**首波掉落必须同时付得起石镐和蓝图**，否则玩家被送回船舱却无事可做，首波就不再是整个设计的支点）；来袭最小间隔 70 → 80 秒。

**5. 地形阻挡区（丘陵）** — `GridManager.blocked_cells` 与 `occupied_cells` 分开存（建筑会来会走，地形不会，所以 `clear_grid()` 不碰它）。**恐龙也改走格子寻路**：开阔地上路线等于 waypoint 本身，所以集群、包抄、防追尾全都没变；**绕地形但不绕建筑**（走向栅栏并啃它——礼貌绕开就等于种木桩没意义）；分散力推进山里的那一步会被撤销，落在山里的攻击站位会被丢掉。走廊钳制随之删除。

**顺带做的几件（实机试玩提出）**：

- **维修**：`Building.heal()` 之前从没被调用过，损坏的建筑只能重建。维修价 = **建造价 × 缺血比例，上取整，按每种资源分别算**——所以永远不会超过重建价，擦伤只花最低一份。一次性交易、做完才结账：中途走开只损失时间，不存在"付了一半的建筑"。
- **右键不弹菜单**。曾经在建筑有多个可选操作时弹小框，那是坏交易：右键太容易误点，每次误点都弹框的代价远大于偶尔想选第二项的收益。右键恢复成**立刻、无后果**（走过去 / 继续建造 / 进船舱），**花钱或有后果的事（维修、拆除）全在左键面板上**，选之前看得见价格。
- **恐龙的攻击有了距离** —— 此前 `_is_target_valid()` 只问"目标还活着吗、能受伤吗"，**从来不问有多远**。同一个洞长出两个 bug：盯上人之后能隔着整张地图一直打到人死；以及够不着目标时僵在原地不动（"恐龙傻掉了"）。现在攻击要在 `DINO_ATTACK_REACH` 之内，状态机和真正造成伤害的地方各查一次，目标走远就放手回去进攻。
- **进攻习惯变成类继承** —— `PackDino`（小型成群）与 `SiegeDino`（大型食肉）。**唯一按物种不同的东西是"它想要什么"，所以也是唯一由子类覆写的东西**：移动、寻路、战斗、死亡、掉落全在基类共享。`Config.DINOS[type].behaviour` 写习惯，`DINO_BEHAVIOURS` 映射到类，**习惯相同的物种直接共用一个类**（迅猛龙和翼龙都是 `"pack"`），再加一种小型群居恐龙是 Config 加一行、零代码。顺手删掉 `targeting` 字符串——它是从来没有人分支判断过的死数据。
- **木尖刺重做成一排小圆锥**（第三次返工；前两次只改尺寸没改结构，所以都没改好）。根因有两个，都不是"数值不够小"：**预览从来不问会造成什么形状**（ghost 永远按满格画，所以 hover 是大方块、落地却变成细条），以及**"拐角 = 满格"这条规则**（造成一片时每根都是拐角，于是整片又全变回大方块）。现在一格栅栏画成 `spikes_per_tile` 根小圆锥，间距 = `footprint / spikes_per_tile`——这个数在格子内部与跨格子边界上**完全相同**，所以一长排是均匀的尖刺线，而不是每到格子边就看见一簇。**任何排布都不再是方块**：成排是一条线，拐角是 L，孤零零一根是个小十字。碰撞盒按同一个轴切出来（成排一个盒，十字两个交叉的盒），横向厚度 = 圆锥直径本身（由 `get_building_thickness` 推导，`thickness` 这个可以和美术打架的数字直接删掉）——**顶在栅栏上的恐龙碰到的就是尖刺**，不再被一个看不见的盒子隔在半米外。预览改问 `Wall.axis_at()`，和落地的木桩问同一个函数，所见即所得。
- **蓝图的半透明其实一直没生效** —— `_update_visuals_progress()` 只看直接子节点，而 `make_body()` 之后所有可见 mesh 都在 `Body` 里，所以**自 make_body 引入以来没有任何建筑真的淡过**，未完工和完工看起来一模一样。一排木桩最需要这个：哪根还没造好得看得出来。（同一个"只看第一个 mesh"的毛病也让受击闪白只闪三根锥里的一根，一并改成整个 body 闪。）
- **清掉幽灵建筑** —— `hut` / `wood_house` / `barracks` 三个定义连颜色带翻译一起删了。它们**从来不在 `BUILDABLE_TYPES` 里，谁也造不出来**，却还挂着早已取消的 `ap_bonus`；住房升级链的新作用至今没定（见 v0.x），等定了再写回来，比留着三份没人能碰的数据诚实。牵出一件更值得记的事：`test_build_system_challenge` 的**多资源支付测试一直拿 `barracks` 当夹具**——也就是说，它验证的是一笔**永远不会发生的交易**，还把 10 木 5 石这两个数字抄进了测试。现在改用防御塔（木 8 + 石 4，真的要付两种资源），价格从 Config 取。
- **8 个测试报 PASS 但什么都没测** —— 根因是同一个：v0.4 删掉了 `LumberHut.gd` 和 `ProducerBuilding.gd`，而这些测试还在 `load()` 它们，拿到 `null`，然后 `null.new()` 直接把方法**中途打断**——断言一条都没跑到，runner 却照样打印绿色的 `[PASS]`。**一个跨了整整一个版本都没人发现的静默失效。** 主语没了的两个（producer 摆料、lumber_hut 的开局曲线）直接删；还活着的六个（进度条、选择环 vs 覆盖环、feedback 层不投影、蓝图显示进度而不是血量、掉落要人来捡、维修按钮派人干活）换成现在真实存在的建筑，价格与解锁从 Config 取。**并且给 runner 加了守卫：0 条断言现在算 FAIL**，附一句说明去看上面的 SCRIPT ERROR——这类静默失效不能再有第二次。
- **一口气造好几排不会卡住** —— 蓝图本来就不挡路（`is_cell_walkable` 视未完工建筑为可通行），所以队列本身是安全的；缺的是**走不到的蓝图要跳过**，否则人会挑最老的那个、走不到、重新规划、再来一遍，一根围死的木桩就冻住整条队列。新增 `GridManager.is_reachable()`，**从蓝图那头淹而不是从人那头**：封死的口袋是有限的，淹得完，答案确定；从人那头淹会在开阔地上一路铺开、永远证明不了任何事（第一版就是这么错的，测试直接证伪）。**预算用尽 = 不知道，按"能到"处理**——猜"不能到"会让人在开阔地图上悄悄停工，代价远大于白走一趟。人卡住时重新问一次，够不到就换下一件能做的活；全都够不到就还是啃最老的那个（他被围住了，撞栅栏至少看得见，而 idle 会把问题藏起来、还会在栅栏被拆开之后继续睡）。

**开局曲线**（全部由测试从 Config 推导，不重写数值）：

```
开局 20 木 = 10 根木桩（每根 1.3 秒）
  ↓ 首波 90 秒后到，2 只迅猛龙 → 2 骨 + 2 肉
石镐（1 骨 + 4 木，8 秒）→ 采石 0.35/秒
烤肉（2 肉 + 2 木，6 秒）→ 防御塔蓝图
  ↓ 战后链条 = 8 + 6 + 11.4 + 12.3 ≈ 38 秒站着不动
防御塔（8 木 + 4 石，12.3 秒）——最短来袭间隔 80 秒，余下的都是走路
```

**首波必须能在栅栏后面打赢**这条硬约束已经从"平衡旋钮"变成了测试盯着的规则——因为链条让第一次战斗成了必经之路。

### 注意点：这是个正反馈环，阻尼只有一半

杀恐龙 → 材料 → 更强防御 → 杀更多恐龙。现有阻尼：**木和石来自资源点，有储量上限、会采光**；**骨和肉只要恐龙还来就是无限的**。所以中期会自然发生一次转折：从"采集经济"变成"战利品经济"。这是特性不是 bug，但意味着**晚期得有事情做**——好在胜利条件已经有了（拆掉巢穴），中后期的目标就是攒够本钱打出去。

### wall 挡路，恐龙绕不过去才打它 —— **已实现**

> 落地情况写在这一节末尾。原始建议与理由保留在下面，因为它推翻了 v0.4 的一个决定，值得留着看为什么。
### 改动建议：wall 挡路，恐龙绕不过去才打它（原始建议）

> 原话：**木墙和尖刺这类都是 wall 类型，wall 类型需要能 block 通路的能力，恐龙进攻优先级低于建筑，高于人，所以一旦路 block 了，恐龙才会攻击 wall，不然就会绕过去。**

三件事：

1. **wall 是一个类别**，木墙、尖刺都属于它，共同的能力是**挡路**。
2. **恐龙的进攻优先级：建筑 > wall > 人。**
3. **恐龙会绕路。** 只有当路真的被堵死，才转而拆 wall。

**这条推翻了 v0.4 的一个决定，而且我认为推翻得对。** 现在恐龙走 `terrain_only` 寻路——**只绕地形，不绕建筑**——当时写在 `GridManager.find_path` 的注释里，理由是"礼貌地绕开玩家刚造的栅栏，就等于种木桩没意义"。

那个理由现在看是错的，它把**栅栏的作用搞反了**：

- 按现在的规则，栅栏是**血包**：恐龙撞上来啃，栅栏的价值等于它能扛多久。
- 按这条建议，栅栏是**漏斗**：它的价值在于**逼恐龙走你选的那条路**。摆在哪里第一次真正重要了，而这正是塔防的乐趣所在——也是 v0.4 加丘陵时想要的那个"隘口"，当时只有地形能做到，建筑做不到。

**实现要点**（留给做的人）：

- 恐龙寻路从 `terrain_only = true` 改成"绕建筑"，但**必须有兜底**：路被完全堵死时，寻路要能返回"到不了"，恐龙才转去拆挡路的那个 wall。没有这个兜底，全封死的基地会让恐龙集体发呆——这个失败模式在 v0.4 的人身上发生过一次（走不到的蓝图），修法见 `GridManager.is_reachable`，**那个函数从蓝图那头淹的思路这里可以直接复用**。
- 优先级要落在 `Dino._find_threat_priority_target` 和它的子类（`PackDino` / `SiegeDino`）里，**不要在基类写 if-else 分支**——种类之间的差异是用子类表达的，这是 v0.4 定的结构。
- `Config.BUILDINGS[type].kind` 已经有 `"wall"` 这个值了，类别现成。

**顺带解决一个现在就存在的矛盾。** v0.5 把木桩改成了**一根一个锥**（你要求的），于是出现了这个状态：**格子在寻路上是堵的，画面上两根之间却有 2 米的空隙**。按现在"恐龙不绕路"的规则这不影响玩法，但它是个看得见的谎。这条建议实现之后，"挡不挡得住"会变成一件**摆位决定的、看得见的事**——空隙就是真的空隙，恐龙真的会从那里过去，玩家自然就知道要把木桩摆密。

**待定**：一根木桩到底该挡多少？现在它声明的 `footprint` 是 2.0（整格），所以 `is_barrier_building("wall")` 为真、两根相邻就没有缝——但画面上明明有缝。要么让声明尺寸跟画面一致（一根挡不住，要摆密才行），要么保留整格（画面继续撒谎）。**这个选择建议和上面这条一起做**，因为它们是同一个问题。

### 落地（v0.5 期间修的一批 v0.4 玩法 bug）

- **小恐龙隔着木栅栏打 cabin。** 修之前量的：迅猛龙**离 cabin 中心 2.03 米**、中间隔着一根木桩，**照样把 cabin 从 10 血打到 9**。
  - `DINO_ATTACK_REACH` 是**写死的 2.2 米、中心到中心、所有物种对所有目标一视同仁**。迅猛龙宽 0.8 米，等于能打到**自己鼻子前面 1.8 米**。这是**第三次**在这套系统里发现同一个错误（前两次是攻击位半径和 `contact_range`）：**一个按某种尺寸调的数字，被套在所有尺寸上**。
  - 现在是 **攻击者半宽 + `DINO_STRIKE` + 目标半宽** —— 咬多近取决于你和你咬的那个东西**两边**。迅猛龙的 reach 从 2.20 掉到 **0.75（越过自身之外）**。
  - 另一半、也是不管数值怎么调都成立的那一半：**咬击不能穿过实心的东西**。
  - **顺带挖出第三个 bug**，它一直躲在前两个后面：**"看见"、"停下"、"够得着"是三个不同的距离** —— 一条随速度伸长的前向射线、一个写死的 1.8 米、和 `attack_reach`。所以恐龙会停在一个它**够不着**的距离上，进入 ATTACKING 状态**一直不掉血** —— 这正是反复被报的那个症状。现在三者是同一个问题。
  - 攻击位也一样：站到位了还够不着的话，**继续往目标贴**，而不是站在位上开打。攻击位是"八只围着一栋楼站开"用的，不是"从这儿一定打得着"的保证。
  - 验证：隔着木桩打 cabin **打不到、不掉血**；咬挡路的那根木桩**可以**；从自己的攻击位、视线畅通时打 cabin **可以**。新测试 7 条，旧代码上 6 条红。

- **导航网格开始烘焙了（大改第二刀的地基）。** `NavMaps` 从**关卡自己的碰撞体**烘两张网格，并且和网格系统的答案**逐条对过**。
  - 之所以能这么干，是因为**每条游戏规则早就已经是一个碰撞层了**：地面/丘陵/资源点 = 1，残骸和塔 = 2，蓝图 = 16，木墙 = 32。于是"**人能穿过自己的栅栏**"从一个要穿过寻路器、直线检测、可达性洪水三处传递的 flag，变成了**两次烘焙的掩码差一个 bit**。
  - **烘焙按 `agent_radius` 算通行宽度** —— 那正是手写封路规则想回答的问题，而那条规则**写了两遍**：第一版只认横平竖直的栅栏，**曲线怎么围都不封**。引擎从来不需要人教它斜线。
  - 对照验证：空地**两边都可达**；围死一圈**两边都说 raid 不可达、人可达**；**只下单没建**的一圈**两边都说通**。
  - `agent_radius` 取成 `cell_size` 的整数倍（0.4 = 4 格），否则烘焙会**向上取整**并警告 —— 半径悄悄变大，等于把玩家故意留的缝封上。
  - **还没切换任何调用方**：寻路仍然走 GridManager 的 A\*。这一刀只把地基铺好并验明，切换是下一刀。

- **栅栏一次只扎一下，不管有几根尖刺够得着你。** 这是同一个错误在这套系统里的**第四次**：**一个按某种布局调的数字，被套在另一种布局上** —— 这次变的是木桩自己的细网格，它们现在只隔 **0.67 米**，而 `contact_range` 必须够宽才能覆盖站在攻击位（**0.91 米外**）的那只。于是**三根**尖刺同时够得着同一只恐龙，各扎各的。
  - 量出来：**0.90 dps，而建造菜单写的是 0.30** —— 正好三倍。迅猛龙 3 点血，**3.5 秒就死**，它正在啃的那根 8 血木桩还剩四分之三。而 `Config.BUILDINGS.wall` 里白纸黑字写着"啃穿这 8 点血之后**活着但只剩一口气**，收尾交给塔或者人"。代码和设计不一致，**菜单没骗人，栅栏骗人**。
  - **修的是规则不是数字**：栅栏造得多密，伤害就不该跟着涨 —— 密栅栏是**更难钻过去**，不是伤害倍率。现在木桩每帧只报告"我贴着你多久"，**由恐龙自己数**，一帧之内几根一起报只算最长的那一次（`Dino.spikes_touch`）。
  - **计时器搬到恐龙身上**，因为**情境在恐龙身上**。木桩是不同时刻造的，各自的 tick 彼此错相位：只拒绝"同一帧的第二根"，手工铺的栅栏照样叠；按经过时间拒绝，则会和木桩自己的定时器**打拍子**，丢掉三分之一本该命中的（量出来 0.19 dps，目标 0.30）。一个吃模拟 delta 的累加器两个毛病都没有，而且**跟着 `Engine.time_scale` 走**，HUD 的倍速正好需要这个。
  - 结果：贴着栅栏 **0.28 dps**（申报 0.30），啃穿一根木桩要 8 秒、总共掉 **2.28 点**，3 点血的迅猛龙**活着出来、只剩一口气**。20 只打 48 根围死的一圈：以前 **15 秒内全灭、栅栏只掉 2 根的血**；现在**啃掉 6 根钻进去，17 只活着** —— 栅栏变回了"消耗 + 拖延"，而不是"全歼"。

- **迁移做完了：网格 A\* 已经删掉，现在只有一个答案。** 三刀切换 + 一刀删除。
  - **恐龙被网格拦住了。** 之前**没有任何东西在物理上拦得住恐龙**——移动是每帧往坐标上加，没有体积扫掠、没有碰撞，木桩有碰撞体但恐龙不在看得见它的掩码上。挡住 raid 的**只有寻路器**，所以它一找不到路就把"尽力而为"的那条交回去，**整队直接穿墙**。量过：48 根木桩围死一圈，**20 只里 17 只进到圈内，一根没啃**。现在同一圈：**一只都进不去**。
  - **"有没有路绕"改成问同一张网格。** 网格系统和导航网格是**同一个问题的两套实现**，而它们**不一致**——不是到处不一致（所以拖了一整个版本才看见），而是**紧贴栅栏那一米左右的一圈**，偏偏那正是答案唯一会被采纳的地方。按 16 个方位 × 9 个距离量了一遍：**5.6 米以外两者完全一致，4.8 米以内每个方位上网格系统都声称有路进去**。于是 raid 走过来时知道封死了，**到跟前就忘了**：20 只全都在出生时判定封死，5 秒后**一只都不认了**，接下来整场都在沿着栅栏找那条并不存在的路，**每擦过一根木桩掉一点血**，15 秒内全灭，栅栏 368 点血只掉了 17 点。这就是玩家报的"**恐龙会在圈出来的地方来回穿梭，进攻不了还掉血**"。改完：5 秒时 17 只里 **14 只在啃**，吃掉 5 根木桩，进去的 4 只是**自己啃出来的洞**。
  - **回到网格上是"纠正"，不是"瞬移"**（`Config.NAV.max_correction`）。`map_get_closest_point` 在关卡加载后的**头几帧一律返回 (0,0,0)**，而且**答案本身没有任何迹象表明它还没准备好**——`is_ready` 不行、区域的 188 个顶点是对的、`map_get_iteration_id` 到 1 的时候答案还是原点。**一波恐龙正好生成在这个窗口里**：一只放在路线巢穴端的迅猛龙，两帧后站在了 cabin 上，目标也改成了 cabin。现在上限一米——比任何诚实的纠正都大（最大的那种是"有人站着的时候栅栏盖起来"，0.71 米），比任何事故都小。
  - **人也走同一张网格**，而"他能穿过自己的栅栏"从一个穿过三处代码的 flag 变成**另一张烘焙里没有 layer 32**。顺带删掉一段**四方向候选点扫描**：走到建筑旁边时逐个试邻居格、逐个寻路、按距离排序取第一个成功的——那是在手写"我这么宽的人能站在这东西旁边哪里"，而烘焙**天生就是这个问题的答案**。建筑在网格里是被挖掉的，所以到它中心的路线**自然停在它旁边**：量出来离 cabin 中心 0.92 米、离木桩 0.20 米，都在施工距离内。
  - **删掉 230 行**：A\*、可达性洪水、最近可站格的环形搜索、路径平滑、视线检测。
  - **测试原来问的是另一套。** 八个测试套件搭的是**没有碰撞体的裸 GridManager**，于是烘不出网格，所有关于"能不能走"的问题都落到网格系统上——"**游戏跑网格、测试跑 A\***"差一点就成真了，而这正是 PackDino 覆盖目标选择那个 bug 藏了一整个版本的同一个缝。现在它们搭真的地基（地面 + 和 `Main.spawn_terrain` 同形同层的丘陵方块 + 一个 NavMaps，见 `test_base.nav_fixture`），或者直接开一个真关卡。
  - **搬测试的过程里挖出它们共同依赖的一个假前提：一格一根木桩不是栅栏。** 木桩 0.62 米、格子 2 米，所以"围一圈"是**八根锥子之间留着 1.38 米的空地**，什么都封不住。网格系统认为那一格被占了所以不可通行，测试就绿了；导航网格**看得见那些缝**。现在按木桩自己的细网格铺**真正的一排**（`test_base.run_of_stakes`），也就是游戏里的铺法、玩家看见的铺法。
  - 其中一条值得单独记，因为它是**游戏规则**而不是测试的规则：**洞得比要钻过去的东西宽**。木桩间距 0.67 米，所以啃掉**一根**留下的缝比迅猛龙还窄，栅栏照样封着。这就是栅栏值得修的原因。
  - **playtest 原来在提前停表。** 会动的东西都在 `_physics_process` 里动，物理是固定 60 tick/秒，而 headless 下的 process 帧是不封顶的——"等 420 帧"其实是**一秒半的游戏时间**。于是 gap 场景报"人走完 16 米里的 12.7 米"，而他**五秒就能走完、只差 0.12 米**，而且这个数**每跑一次都不一样**。改到物理时钟上：**15.9 / 16.0**。raid 场景同一个毛病。

- **恐龙的局部避让换成引擎的了（`NavigationServer3D` 的 RVO2）。** 这是"**有内置的就用内置的**"这条原则（`AGENT-TASKS.md` 铁律 8）的第一次兑现，也是它的由来。
  - 删掉了手写的四件套：分离力、前车限速、从哪边绕、以及为了让前三者不来回翻转而加的转向阻尼。它们加起来 **-217 行**，并且产出了这一整轮里最难缠的 bug：人群里互相让到**零速死锁**、两只恐龙 25 秒内**换边 47 次**、按距离采样的直线检测**跳过整整一格**。
  - **用的是 server 而不是 `NavigationAgent3D` 节点**，这不是偏好：节点版的 safe velocity **每一帧都返回零**，试过所有参数、有窗口和无窗口都一样；同一个请求走 server 直接返回要的速度。两边都量过才选的。节点本来就只是 server 的一层壳。
  - **一个有意保留的手写例外**：RVO 调的是**速度**，所以它能让碰撞不发生，却**解不开已经重叠在一起的两个身体**——同一点生成的两只会永远叠着，量出来间距 **0.000000**。引擎没有给导航代理提供解重叠，所以那一小段留着，并且写清了理由。第一版拿旧的 `DINO_SEPARATION_MIN_DIST`（1.15m）当阈值，比 solver 想维持的距离还大，两边打架，**20 只里有 1 只被钉死整整 20 秒、每次都如此**——改成用两个代理**自己的半径**。
  - 结果（同一套探针）：弧形栅栏 **17~20/20 到达、0 停滞、掉头 2~4 次**；旧的手写版最好一次是 18/20。

- **人可以穿过自己造的栅栏，别的谁都不行。** v0.5 之后栅栏终于真的能封路了 —— 然后它把**造它的人**也一起封在外面，而游戏里**没有门**。绕着自己营地插一圈木桩，就等于把自己锁在外面，唯一的进去办法是拆掉自己的活儿。
  - `wall` 现在有**自己的物理层**（`Config.LAYER_WALL`），人的碰撞掩码**只**漏掉这一层 —— 残骸和塔照样挡他；寻路那边也加了 `walls_are_open`。**两半都得有，缺一个都不行**：只改物理不改寻路，他会绕一大圈去走一个本来能直接穿过去的地方（看起来就像寻路坏了）；只改寻路不改物理，他会一头撞上去。
  - `walls_are_open` 只开 **wall**。而且判的是**那一格里站着的所有东西**，不只是格子登记的那一个——一根木桩和残骸同格，不能让残骸变得能穿过去。
  - **代价确认过**：恐龙那边**一点没变**（射线掩码同时包含两层）。弧形栅栏 **20 只里 18 只绕过去、啃墙 0 次**；围死的一圈仍然 **4 只在啃**。
  - 连带影响：**"人被围死"这个状态现在不存在了**。`_reachable_among` / `_abandon_unreachable_building` 那套机制还在，但只对**地形**有意义了——相关测试已经从"用木桩围"改成"用山围"。

- **"恐龙在圈出来的地方来回穿梭，进攻不了还掉血" —— 量出来是 25 秒里 47 次掉头、掉 18.7 点血（迅猛龙总共 3 点）。** 三个原因，前两个是**同一个错误**：**一堆数字是按"每个建筑都占满 2 米格"调的，而木桩被我改成了 0.62 米**。
  1. **攻击位是从建筑中心算的固定半径 1.6 / 2.6 米。** 对着一根 0.62 米的锥，那是站在**离它 1.3 到 2.3 米**的地方 —— **这就是反复报的"站在木尖刺前（有一段距离）"**，而且在目标选择规则上怎么修都修不好，因为根本不在那儿。现在按**建筑自己的尺寸**算，量的是**离表面**的距离：`DINO_STANDOFF_INNER/OUTER`。2 米的建筑代回去正好还是 1.6 / 2.6 —— 有测试钉住这一点，否则这就是打着修 bug 旗号的重新平衡。
  2. **`contact_range` 是写死的 2.0 米。** 每根锥都伤害两米内的一切 —— 一根 0.62 米木桩周围**四米宽的伤害场**，把只是从缝里**路过**的恐龙活活磨死。现在也从尺寸推导（1.3 米），只够到贴着尖刺站的那个。
  3. **转向是每帧从零重算、整个替换的**，所以任何会变号的项（从哪边绕、邻居往哪推）都能**一帧之内把恐龙掉个头**。挤在角落里就是永远解不开的原地打转。现在速度是**转过去**而不是**瞬移过去**（`DINO_TURN_RESPONSE`），并且**"从哪边绕"一旦选定就不再改**——重新考虑到一半，正是撞上去的原因。
  - 转向阻尼是**两个指标一起调**出来的，因为它确实有代价：6.0 时打转没了、进攻也没了（20 只里只有 2 只到达，原本 13）；20.0 时打转维持在 6.0 的水平，而进攻速度和完全不阻尼一样快。
  - 结果：**掉头 47 次 → 0~2 次**，掉血减半。`tools/playtest.gd` 的 `raid` 场景现在也报掉头次数——一支来回穿梭的队伍从来不"静止"，只看停顿的计数器会说一切正常。新测试 9 条，旧代码上 9 条全红。

- **"人完全不动了" —— 不是寻路，是他被自己盖的木桩困在里面了。** 量出来才看清：人报告的状态是 **MOVING、速度 4.0**，八秒里走了 **0.13 米**。他在原地踏步，手里还握着一条完全正常的路线。
  - 根因：**可以把木桩盖在自己身上**。`can_place_building` 从来不检查有没有单位站在那儿。盖好的建筑是 static body，而**已经重叠进去的身体，`move_and_collide` 永远推不出来**——所以他会以全速顶着它走到天荒地老。重新规划路线一点用都没有，路线从来不是问题。
  - v0.5 之后这事变得极容易误触：木桩 0.62 米宽、吸附到三分之一格，"贴着我"和"在我身上"只差半米。
  - 两条修复，一条是规则一条是网：**放置时拒绝把建筑放在有人站的地方**（按两个身体半宽之和算间隙，贴着可以、重叠不行，而且判的是**吸附之后真正落点**，不是鼠标点的位置——只判点击的第一版让木桩照样落在人身上）；以及**已经被困住的人会被推出来**。
  - 顺带挖出 `get_all_buildings()` **只返回每格一个建筑**。细格放置之后，三格里九根木桩只报三根。所有问"地图上有什么"的代码都只拿到三分之一——包括人的建造队列，这就是为什么和已建好木桩同格的蓝图会一直停在 **0%**。
- **一条真实的 flake 查清了**：`test_multiple_concurrent_dinos_attacking_same_wall` 四次全量跑里红了两次。不是玄学——恐龙在人群里不再死锁之后会**漂移**，而那条测试把 8 只摆在 0.7 倍攻击距离处、然后等了两帧，偶尔有一只漂出射程、少打一下。测试同时在测两件事。摆好之后冻住它们，连跑三次 693/0。

- **"恐龙站在木尖刺前一段距离就不动了" —— 第四次报，这次找对了。** 前三轮的修复都是真的、都量过，**但量的是没有任何一次进攻跑过的代码**：
  1. **规则写在了子类会覆盖掉的那个方法里。** `PackDino` 和 `SiegeDino` 都整个重写了 `_find_threat_priority_target`，所以"只咬真正挡路的墙"这条**只对基类生效**。而 `raptor` 的 behaviour 是 `pack` —— **每一波里的每一只迅猛龙都绕过了这条规则**。用游戏真正生成的脚本量：一道两端都开着的栅栏，**20 只里只有 2 只过去了**；没过去的停在**离木桩 2.7 米**的地方——那正是 `DINO_ATTACK_SLOT_RADIUS_OUTER`，也就是报告里的"有一段距离"。现在规则挪到了子类**够不着**的地方：物种通过 `_preferred_target` 说它"想要"什么，能不能要由基类判。有测试扫描 `scripts/entities/`，**除 `Dino.gd` 外谁都不许定义 `_find_threat_priority_target`**。
  2. **给前面的恐龙让路可以让到速度归零。** 在人群里这就是死锁：每只都被前面那只停住，**谁都没有速度可以挪开**。20 只量出 9 只站着不动，最久的一只连续 10 秒。**5 只的时候几乎看不出来**——而我之前每一个探针都用 5 只。**人群和排队是两个不同的问题，进攻是人群。** 现在有下限 `DINO_CROWD_MIN_THROTTLE`。
  3. **"侧身让开"是写死世界 X 轴的。** 沿 Z 轴来的进攻恰好对，沿 X 轴来的就是**往要躲的那只身上撞**，斜线更是一路都错。现在按行进方向求垂直。
  - **例外是有意保留的**：`SiegeDino.gd` 写着它"从防线**中间穿过去**而不是跟防守者打"、咬"挡在它面前的任何东西"。把规则做成universal会让 siege 这个词只剩注释，所以"绕不绕墙"是**物种属性**（`walks_round_walls()`）：群居绕，重型不绕。
  - `tools/playtest.gd` 的 `raid` 场景本来就是为这条报告写的，**它两次都没抓到**：用了 5 只、用了基类。现在默认 **20 只**、用 `Config.get_dino_script_path("raptor")` 真正生成的类，并且报的是**有人站着不动多久**而不是平均推进——平均值恰好会藏住被报告的那种情况。修好后：弧形栅栏 **20/20 全部通过、啃墙 0 次、最长停顿 0.1 秒**。新测试 9 条，**旧代码上 8 条全红**。

- **半成品建筑点不回来了。** 蓝图以前是 `collision_layer = 0` + 碰撞体 disabled。这确实做到了"不挡任何人"，同时也让它**对点击射线完全隐形**——走开之后就再也点不中，活儿就烂在那了。现在蓝图有自己的一层（`Config.LAYER_BLUEPRINT`），**拾取射线看这一层，别的谁都不看**：能点，仍然谁也不挡（有测试逐条验证它不在恐龙的射线掩码、人的碰撞掩码、塔的探测掩码里）。
- **只是"下了单"的栅栏会挡住恐龙。** 细格把蓝图算成占用，而封路判定把"占用"当成"实心"。**下单不等于建好**，恐龙被一排还不存在的木桩拦住了。现在分成两个问题：**占位**（蓝图占，所以同一格不能再插一根）和**实心**（蓝图不算）。顺带修掉了同一个函数里的一个坑："没有实心的"曾经和"没有细格记录"走同一个分支——前者该是"能过去"，后者才是"什么都不知道，按整格算"。
- **点得中哪一根。** 以前右键先问 `get_building_at(格子)`，而一格里挤着好几根木桩时，格子只记得其中一根——**点半成品的那根，动作落在旁边已经建好的那根上，于是什么都没发生**。现在按**世界坐标点**查（`building_at_point`），先看细格。
- **鼠标悬停高亮。** 选中单位之后，光标下面的东西会描一圈轮廓，颜色和选中圈**刻意不同**。十八米高的视角加 0.62 米的木桩，在这之前根本看不出点下去会中哪一根、甚至会不会中。圈的大小形状取自那个东西**自己的**选中圈，所以悬停和选中描的是同一个形状，只有颜色不同。
- **测试框架的一个洞（顺手发现的）**：GDScript 的运行时错误**不会让测试失败**，它只中断那个函数，而 runner 只数断言——**已经断言过几条的测试照样报 PASS**。当场抓到 4 处：两个测试因为 `LumberHut.gd` 早就删了而在 `new()` 上炸掉、**沉默地半途而废**；两处把 `Dictionary` 和 `int` 比大小。现在顺序比较的断言会**先查类型**，类型不匹配就是一条正常的失败；跑测试的命令也写进了 `AGENT-TASKS.md`：`grep -c "SCRIPT ERROR"` **必须是 0**。

- **恐龙在栅栏前"停下来又不进攻"的第二轮。** 报的是一道两端都开着的弧形栅栏：恐龙既不绕过去打 cabin，也不打栅栏，停在离栅栏挺远的地方。**三个独立原因，没有一个是那条规则本身**——所以光有规则修不好：
  1. **规则管住了"咬"，没管住"锁定"。** 恐龙会给一根它压根不打算咬的木桩**占一个攻击位**，走过去，因为"有路绕"被释放，然后再占一次。结果就是在离栅栏一个攻击位半径的地方来回蹭——**那个"停得比较远"就是攻击位半径**。
  2. **"路被封了吗"问的是当前目标，不是终点。** 目标永远可达（等你咬它的时候你就站在它旁边），所以这么判的话**任何墙都不算挡路**，包括把核心围死的那一圈。现在问的是**去的地方**。
  3. **直线检测是"采样"的，会漏格子。** 每米采一个点，可以整个跨过一格的角。于是恐龙被告知"前面是通的"，以 4m/s 撞进山坡，被推出来，下一帧再来一次——**永远**。这一条**跟栅栏完全无关**：空地上、进攻路线两侧那两座山旁边，一直就在冻恐龙。改成真正的网格遍历（DDA），并且问的是**和寻路器同一个问题**（以前直线检测把"被占用"当成实心，而 A\* 会直接从那格穿过去——两边不一致，走的人既不绕也不停）。
  - 另外加了**兜底**：撞上地形现在是**贴着滑**，不是原地停死。以后任何检测出偏差，最坏是蹭着山坡继续走，而不是一只恐龙从此退出游戏。
  - 还发现**曲线栅栏封不住**：上一版"整行或整列填满才算封"只认识横平竖直的栅栏，而人画的是**弧线**——斜着穿过一格的两三根锥，不占满任何一行一列，看着严丝合缝却什么都不挡。现在问的是老实的问题：**这格还能不能穿过去**（自由细格的连通性，南北和东西都得通才算开）。
  - `tools/playtest.gd` 加了 `raid` / `raid raidsealed` 场景。弧形：**18.4 米走了 13.3 米，啃墙 0 次**；围死：**啃墙 4 只**。新测试 8 条，其中 6 条在旧代码上会红（`test_08` 旧代码下是 **0.0 米**）。
  - 代价：**5 条老测试断言的是"恐龙会停下来吃掉一根孤零零的木桩"**，也就是 v0.4 那条规则本来就要删掉的行为。它们之所以还绿着，正是因为占攻击位那条分支绕过了规则——**bug 在替测试撑着**。已按新规则重写。

- **圆锥和丘陵之间看得见的缝，现在真的能走过去。** 两件事在说"不行"，而且是同一个错误——**木桩是按它那格的大小声明的，而不是按木桩的大小**：
  1. 碰撞体是 **2 米的盒子**，装着一根 **0.62 米**的锥，所以人被一个看不见的东西挡住；
  2. 第一根木桩落地就把整个 2 米格标成被占，寻路**根本不去试**。
  现在木桩只有一个尺寸（`spike_diameter`，锥、盒子、占地是同一个数），**封路的是一"排"**：细格里凑满一整行或一整列，也就是一条横穿这一格、中间没有空隙的锥线——那才是玩家看得出来是栅栏的东西。一根就是绕过去。
  - 这条把 v0.4 记在 `Wall.gd` 里的那笔交易翻掉了。原话是"把 footprint 缩到和锥一样大……会导致一根木桩什么都挡不住，这是没人要求的玩法改动"。**它被要求了，以 bug 的形式。**
  - 格子仍然被第一根木桩**认领**（点击、查询、归属都还要有答案），但**认领不等于封死**。
  - `tools/playtest.gd` 加了 `gap` 场景：真的地形巨石 + 一根木桩，人被命令穿过去。跑出来 **16.0 米走了 15.9 米**。新测试 10 条，验证过：把旧规则放回去，其中 3 条立刻红。

- **AP 这次是真的删掉了。** v0.2 写着"行动点彻底取消"，实际做的是**藏起来**：标签设成不可见、`ap_cost` 改成 0，而 `current_ap` / `max_ap` / `infinite_ap` / `spend_ap` / `can_spend_ap` / `reset_ap` / `recalculate_max_ap` / `ap_changed` 信号 / `BASE_AP` / 每个建筑的 `ap_cost` / HUD 的 `APLabel` 和两条文案**一个都没动**。三个版本之后它还在被扣、还在失败时回滚、还有一整个测试套件在断言"AP 从 3 扣到 2"。**一个东西只有在没人能调用它的时候才算删掉了**，现在没人能了，并且**有测试扫源码禁止它回来**（扫 `scripts/` `scenes/` `translations/` `project.godot`，验证过：把 `ap_cost` 加回去测试立刻红）。
  - 保留了 `recalculate_max_ap` 里唯一在干实事的部分：清掉已经被 free 的建筑，现在叫 `_prune_buildings()`。
  - 测试删了 18 个（整套 AP 语义），改了 20 个文件。`test_economy_ap_challenge.gd` 四个测试里三个是 AP，改名 `test_economy_build_cycle.gd`。

- **恐龙不再"站着不动也不进攻"。** 根因不是寻路，是**缺一条规则**：撞见建筑就咬，不管有没有路绕。于是一队恐龙遇到一道**留着口子**的栅栏，四只站在那儿啃门柱。现在 `_should_bite()`：**wall 只有在真的挡住路时才值得咬**，否则绕过去；塔和残骸不受这条约束，它们本来就是目标而不是障碍。绕不过去时 `_building_in_the_way()` 指名挡路的那个，所以**"既不动也不打"这个状态没有了**——要么有路走，要么有东西咬。
- **路上被盖了建筑的 waypoint 会跳过。** 之前路线沿着 path column 走，玩家在上面插一根木桩，整队恐龙就走到它近侧停住：不进攻（有路绕），也不走（要去的地方在建筑里）。
- **恐龙寻路改成绕建筑**（`terrain_only` 去掉）。v0.4 当时的理由是"礼貌绕开栅栏就等于种木桩没意义"——**那句话把栅栏的作用说反了**：绕得过去的栅栏是**漏斗**，绕不过去的才是血包。让栅栏不被无视的是上面那条规则，不是让恐龙瞎。
- **人现在会自己找路。** 三个独立原因，没有一个是寻路算法本身：
  1. `_raycast_ground` 只打 **layer 1**，而 v0.5 把**看得见的地面**扩到了山谷壁、**碰撞体**还停在 44 米的可玩区——点击视野里大半的地面**打不中任何东西，于是根本没下命令**。现在射线落空就跟 y=0 平面求交，**点哪都有结果**。
  2. 目标格子在建筑/丘陵/树里时，只找了**八个邻居**，而丘陵正中间八个邻居也都是山，于是放弃并返回一条**直线**。现在按环向外找最近的可站位置。
  3. A* 失败时返回**直线**——直接把人送进最近的墙里，卡住、重规划、又是同一条直线。现在返回**已经找到的最接近目标的那条路**，走到能走的最近处停下。顺带把迭代上限从 800 提到 4000，因为 v0.5 之后可点击的地面大了很多。
- **版本号真的只有一个来源了。** `project.godot` 说 v0.4.1，`HUD.gd` 兜底写着字面量 `"v0.0"`，`HUD.tscn` 里两个标签存着 `"v0.2"`——其中**结算界面那个从来没有任何代码给它赋过值**，所以"Defend Dinosaur v0.2"挂了三个版本。现在两处都从 `AppInfo` 取，兜底值是 `"unknown"`（**故意不像版本号**：一个看起来合理的字面量和真相无法区分），并且**有测试扫描 .tscn 禁止任何烤进去的版本字符串**。
- **窗口设置不再出现两次。** 暂停菜单里每一行加进 `page_vbox` 的控件默认在每一页都显示，`window_row` 漏了写可见性，于是主菜单和设置页各出现一次。

### 待定（交付后仍未拍板）

1. ~~**水还留不留**~~ —— **已定：留着**。但**形状要改**：现在是地上一小滩，而这是恐龙时代，应该是一条**河**。一小滩水读起来像个水坑，一条河是地形、是天然的屏障、也是一个值得把营地建在旁边的理由——它同时解决"水没有去处"里的一半，因为**位置本身就是价值**。（做法待定，排在木栅栏和树的模型之后。）
2. **v0.x 记的"恐龙训化 / 超人"在这个闭环里还有没有位置**？"建筑大师"实际上已经等于厨房线走到底。
3. **蓝图全部解锁之后，肉的去处** —— **推迟到后面的版本再想**。蓝图是永久解锁，列表见底那天肉就没用了；这个问题不拦着 v0.5，留到那时候再拍板。
4. ~~**工具要不要耐久**~~ —— **已定：不要**。做出来的是一个永久标志，不是物品；测试直接禁止配方携带 `durability` / `stack` / `count`。

---

## v0.5 迭代规划与 TODO 清单

> 视觉 MVP。一次垂直切片：**不扩玩法**，只把已经有的东西换成一套统一语言的美术。尚未开工。

**目标：做出一个统一风格。** 验收不是"每个模型好看"，而是**八样东西放在同一个画面里，看起来像出自同一个游戏**。

垂直切片的意义在于：把一个场景做到最终质量，比把八十样东西做到半成品有用得多——它能立刻回答"这套风格在**游戏自己的相机下**成不成立"，然后才谈后面是买、是外包还是自己做。

### 范围（就这八样）

1. **一种恐龙**
2. **船舱**
3. **人**
4. **木栅栏**
5. **树木**
6. **地形**
7. **石头**
8. **巢穴**

### 风格要先落纸，否则"统一"无法验收

需要先定下来的四件事：**一套调色板**、**一个多边形密度档次**、**同一种着色方式**（平面着色 / 手绘贴图 / 写实 PBR 三选一）、**同一个尺度基准**（人多高、一格多大）。

仓库里已有的 `assets/models/t_rex.glb`（`tools/generate_dino.py` 脚本生成、低面、平面着色、带骨架和 5 个动画）是现成的候选基准——它还从来没有被接进游戏里过。

### 动手做美术之前必须先做的三件代码事

1. **让代码不关心美术**。现在所有外形都是代码堆出来的几何体：`Building._build_body_mesh()`（方块/尖桩）、`Dino._ensure_components()`（方块）、`Hero._ensure_components()`（方块）、`ResourceNode._ensure_components()`（圆柱）、`Nest._ensure_components()`（方块 + 入口）。要先改成**从 Config 读一个场景路径**（`Config.BUILDINGS[type].mesh_style` 已经是个现成的钩子），否则每接一个模型都要改一次逻辑。
2. **动画状态机接口**。人和恐龙在代码里早就有状态（IDLE / MOVING / BUILDING / HARVESTING / ATTACKING / DEAD），但**整个项目没有任何地方驱动动画**（没有 `AnimationPlayer` / `AnimationTree` 的调用）。少了这一层，换上模型只会得到八个僵直的雕像——**比方块还糟**。
3. **场景环境**。`scenes/Main.tscn` 目前**没有 `WorldEnvironment`**：没有天空、没有环境光、没有雾、没有 SSAO、没有 bloom、没有色彩分级，只有一盏平行光。统一风格有一半来自光照，这个必须先有。

### 两个容易漏掉的细节

- **资源点要成对**：木头和石头**会被采光**（`ResourceNode.is_depleted`），所以树和石头各需要**两个状态**（完整 / 采空），不能只做一个。
- **地形分成两半做**：不可建造 / 不可通行的**规则**在 v0.4 落地（`GridManager` 的静态 `blocked_cells`，那时用占位方块），v0.5 只负责让它**看起来像丘陵**——真实高度的网格、材质、以及**外观边界要和格子边界对得上**，否则玩家会看到"明明能走过去却走不过去"。

### 验收

在**游戏自己的固定相机**下截一张图，八样东西同框。换句话说，**验收标准是一张截图，不是一个清单**。

### 改定（2026-09-22）：风格化·茂密低多边形，沉浸感靠氛围而不是贴图精度

**下面"写实 PBR"那一节作废，留着是为了看得出为什么改。** 它一直没落地——游戏里实际做出来的地形和植被都是低多边形——而决定性的理由是：**没有任何免费的、带动画的写实恐龙**。写实环境里站着低多边形恐龙，风格会撞车；那一节自己也写过"做到七成的写实读起来是坏了"。

目标换成**远古恐龙时代的风貌，让人身临其境**，而这件事的杠杆不在贴图上：

- **氛围**：湿热的温室世界——暖而不橙的阳光、乳白的湿雾（体积雾，引擎自带）、深苔绿的谷底。第一版试过琥珀色的雾，整个山谷读起来像日落的稀树草原：**琥珀色的雾是灰尘，灰尘是沙漠**。湿气散射出来是近乎白色的。
- **植被**：这是最大的杠杆。侏罗纪的地面铺满蕨类和木贼，上面是树蕨、苏铁、南洋杉——**草在那个年代几乎还不存在**。现有美术包里的树都是温带的橡树松树，放进来读起来就是"现代森林里有几只恐龙"，所以植被用 Blender 程序生成（它们本来就是由重复的羽片组成的几何形，正是程序生成擅长的）。
- **地貌**：火山、河流、峭壁。
  - **[x] 丘陵是一丛火山岩柱**（`tools/generate_props.py` 的 `rock_formation`，三种，按格子定选哪种、朝哪边），脚下一层低矮的土丘：边缘与地面齐平，颜色和材质就是谷底本身，所以看不出方框。原来的余弦圆顶单独立着，就是一顶灰帐篷。
  - 两个教训。**岩石颜色按 sRGB 写，数字要比直觉亮**：第一版 0.14 换成线性只有 0.018，比煤还黑，逆光时每块石头都是剪影，调环境光和 SSAO 都救不回来（量过：分别只差 3 和 1 个色阶）。**美术缩放、旋转之后要按最终位置量**：岩石拟合之后又绕自己的原点转，角伸出格子 14 厘米，而旧测试量的是网格自己的空间，看不见。
  - **[x] 天际线上两座冒烟的火山**（`scripts/fx/Volcano.gd`，数字在 `Config.VOLCANOES`）：大的一座就在开局视角的方向，把镜头往上一抬就能看见；远的一座藏在山脊后面，只露出烟——远近两层，比一个地标更有纵深。锥体脚在谷底以下，所以它是从谷外的地里长出来的，而不是摆在一块板上；什么都不碰撞，不在格子上。
  - 教训：**粒子的 turbulence 在 Godot 里是每帧把一部分速度方向掰向噪声场**，比例大到看得出来时，烟团几帧之内就忘了哪边是上，全挤在火山口打转——第一版的烟柱根本没升起来。现在不用 turbulence，风就是侧向的"重力"，烟先直升再被吹弯。另外远景要**按雾后的颜色调**：雾会把一半以上的颜色换成雾色，照近处调的灰烟和天空一样亮，等于不存在。
  - **[x] 谷壁上的柱状玄武岩峭壁**（`tools/generate_props.py` 的 `basalt_cliff`）：一段段六棱柱紧挨着，后排高、前排低，平顶一级级往下，顶上长苔，脚下有碎石；在谷壁最陡的那一圈断断续续地排着，混在树里，每段都转过来**面朝谷心**，并且**埋进坡里**，下坡那一侧不会悬空。第一版是把丘陵的岩柱放大几倍，结果圆滚滚的读起来是巨石，不是峭壁。
- **巢与哨位**（`tools/generate_props.py` 的 `nest` / `sentry`）：
  - **[x] 恐龙巢**是一个扒拢起来的低土丘：顶上凹坑里一窝带斑点的蛋，沿边一圈折断的树枝和枯蕨，脚下朝着场地的一个洞口——兽潮就是从这里出来的——门口扔着两根骨头。原来前面贴着的那块黑方块只在占位模型上才有：模型自己有洞口，再贴一块就是两个嘴。
  - **[x] 自动哨位**是从残骸上拆下来的机器（白色外壳、橙色警示带、双管、红色的眼）装在一个用木头绑起来、底下垒石头的架子上。**机头是单独的节点，会转过去对准它正在打的那只恐龙**（`Tower.gd`，转速在 Config）——看它转向谁，玩家就知道它挑了哪只；子弹从枪口出来，而不是从架子中间。转到任何角度都不会伸出自己的占地。
  - 顺带修了一个时序抖动的老测试：`test_11` 手动喂木桩"0.4 个 tick"的接触，但木桩自己的物理帧也在跑，前面的测试加载模型一慢，引擎一次补跑好几个物理帧，就凑够了一整个 tick。现在测试里把木桩自己的物理帧关掉，只算测试自己喂的。
- **[x] 地上的资源堆**（`tools/generate_props.py` 的 `drop_*`）：每种资源一个自己的样子——劈好的木柴垛、一堆采下来的石头、几根骨头、一块带骨的肉、一罐水。原来全是涂了资源颜色的方块，绿方块是木头。每堆转一个黄金角，一地的堆不会朝同一个方向排队，而且每次运行都一样，截图还能对比。
- **[x] 崩溃：单击放一根木桩就崩**（`Trying to assign an array of type "Array" to a variable of type "Array[Vector2i]"`）。拖拽建墙时写的 `_run_to(...) if _dragging else [_drag_from]`——**三元表达式里的数组字面量不会按接收变量的类型来推断**，没拖动的那一侧是个无类型的 Array，赋给 `Array[Vector2i]` 就是运行时错误。拖拽的测试全绿，因为它们**绕过了输入处理**：手动设好拖拽状态、自己造一个带类型的数组去调 `_commit_run`，偏偏出错的就是把点击变成那个数组的那一行。
  - **对崩溃的防线，三层**：①测试运行器用引擎自带的 `Logger` 钩子（`OS.add_logger`，`tests/script_error_watch.gd`）盯住每一条 SCRIPT ERROR，**哪个测试运行期间出了脚本错误，哪个测试就失败**，不管它的断言怎么说——以前全靠事后看日志；②新的输入测试一律**以 InputEvent 的方式走真实的输入处理**（单击、连点、拖拽、造塔、点不能建的地方、各种取消、选中和指挥现代人、所有按键、滚轮和中键拖动、以及一次走完引擎完整输入管线的点击），不再替输入处理去调它内部的函数；③一个扫源码的测试，禁止把三元表达式里的数组字面量交给带类型的数组。把原来那行放回去验证过：①②③都会红。
  - 顺带：换建造工具时会丢掉进行到一半的拖拽，拖的是木桩就不可能松手变成一排炮塔。
- **角色**：Quaternius 的 CC0 带动画模型，同一个作者，风格统一。
  - **[x] 换上了**（2026-09-23，经用户同意下载）：霸王龙、迅猛龙用 Animated Dinosaur Pack，现代人用 Ultimate Animated Character Pack 的 Worker（戴安全帽的工人——正是"现代人"）；四种食草恐龙一并转好，留给谷壁上的兽群。翼龙那个包里没有，继续用生成的。转换在 `tools/convert_quaternius.py`，原文件原样留在 `assets/source/quaternius/`。
  - 一路上的坑，每一个都导出再读回来验证过：**FBX 的材质 alpha 是 0**，导出成 alpha 裁剪，恐龙在场上、有骨骼、在动画——**整个是隐形的**；**所有模型都朝反方向**，而游戏里一切都用 look_at 转身；**FBX 动画里给骨架对象本身也打了关键帧**（缩放 3、无旋转），导出时覆盖了我设的朝向和缩放，游戏里一播动画就会变回三倍大、背对前进方向；**网格相对骨架的缩放**让 Godot 把 30 米的霸王龙框成 93 米，而 VisualLibrary 按框来缩放；工人是 **T-pose 绑定**，框的宽度是张开双臂的宽度。长尾巴的恐龙塞进碰撞箱会只剩三分之一高，所以**恐龙和现代人按身高适配**，尾巴伸出碰撞箱是正常的。
  - 工人原本的肤色是 0.013（几乎纯黑），配上白色的"Face"材质就是一个黑脸白眼的剪影——既读不出五官，又是个不该有的漫画形象。改成暖色的中等肤色，五官可读。想换别的肤色，改 `convert_quaternius.py` 里的 `RECOLOUR` 就行。
  - 顺带：**哪些动画循环**以前谁都没管，走路播一遍就定格。现在由 `Config.ANIMATIONS.looping` 统一决定，对所有模型生效（包括生成的翼龙）。新增的测试会检查游戏加载的每个模型都**看得见**（没有全透明或被裁掉的材质）、恐龙朝着前进方向、身高等于声明的高度、该循环的循环。


### 已定：写实 PBR，目标是"风格化的写实"而不是照片级（已作废，见上）

**选它的理由**："这样保持一致性可能容易一些"。这个直觉对了一半，而且是便宜的那一半：PBR 有物理基准（albedo 有正确区间、roughness/metalness 有确定含义、尺度按真实米数），所以两个不同来源的资产**只要都按 PBR 规范做、放在同一套光照下，就会自然地坐在一起**。风格化则要先凭空发明一套调色板和一套着色规则，再靠纪律把每个资产按住——"统一"全靠人守。

**错的那一半，以及对策**：

- **一致性在 PBR 里主要由光照和后处理决定，不由模型决定。** 光照对了，模型参差不齐也能读成一体；光照不对，模型再匹配也像贴上去的。所以"做出统一风格"的工作量从"发明一套风格"挪到了"把环境光照做对"——也就是下面第三件代码事。**这是这一档里最该先做、也最容易被跳过的一步。**
- **写实没有下限，风格化有。** 一个干净的低面平面着色场景，面数再低也读起来像"故意的"；而**做到七成的写实读起来是"坏了"**。这就是"怕没法做的够写实"那个担心——它是对的，而且它是这一档**唯一真正的风险**。
- **对策：目标定成"风格化的写实"（stylized realism），不是照片级。** 保留 PBR 管线（metalness/roughness、真实尺度、真实光照），但不追照片级细节：中等面数、克制的贴图细节、一套刻意控制的环境。这样**落差的方向是"风格化"而不是"失败"**——有下限可兜。
- **这台相机帮了大忙。** 游戏是固定斜俯视、永远不贴脸，所以毛孔级皮肤细节和精细法线在这个距离上**几乎看不见**。真正决定"像不像真的"是**剪影、比例、阴影接触、环境光遮蔽、远景雾**。换句话说："够写实"这道门槛在这台相机下比担心的低，而"光照一致"这道门槛比想象的高——力气要花在后者。
- **写实把动画的门槛抬得最高。** 一头写实恐龙动得僵硬，比一头低面恐龙动得僵硬难看得多。而这个项目**目前一行驱动动画的代码都没有**。所以第二件代码事（动画状态机接口）在这一档里不是可选项。

**连带的两个决定**：

- **买，不要自己画。** 写实 PBR 的资产包极多，而"手画写实贴图"正好是最不会的那部分。硬规则：**环境那一套（地形、树、石头）尽量出自同一个包或同一个作者**——跨作者的写实资产冲突起来最难救（texel 密度不同、photoscan 的色彩分级不同、尺度约定不同），而风格化资产的冲突改起来便宜（重新上色即可）。
- **`assets/models/t_rex.glb` 不再是风格基准。** 它是低面、平面着色的，和写实 PBR 不是一档。它仍然有用，但用途变了：**它是骨架与动画的测试夹具**（带骨架和 5 个动画），用来验证第二件代码事能不能跑通，不是用来定风格。

### 待定

1. **买哪一套资产包**（环境一套、恐龙一只），以及能不能凑出"同一个作者"。
2. 恐龙要不要外包——写实恐龙是这八样里唯一明显做不到"买了就能用"的（要绑定、要动画、要和 `Config.DINOS` 的尺度对齐）。

---

## v0.x 远期构想（不排期，仅记录）

> 以下条目为设计讨论中明确推迟的方向，不排期，仅作记录。未经确认的想法不写入本节。

- **[远期] 玩家自定义键位与控制设置面板**：允许玩家在设置面板自由更改走位键、建造键、暂停键、视角旋转键等快捷键绑定（已在 `Config.CONTROLS` 预留 SSoT 数据字典）。依托 v0.2 第 8 项建立的选项入口扩展。
- **[远期] 巢穴内部结构**：v0.1 的巢穴是不可进入的黑盒，只做外壳、出入口与外部守卫。内部（房间、蛋、孵化、巢穴核心等）留待本节设计。
- **[远期] 现代人属性成长与装备科技树**：移动速度、建造能力（施工速度）、采集能力（采集速度）、攻击力、生命值五项核心属性。前期靠建筑防御，后期靠科技升级装备，让现代人自己也能打败恐龙。**硬规则：升级路径必须依赖建造**——属性提升来自装备，装备来自建筑。
- **[远期] 人与塔的职责分离**：塔必须永远拥有比人强、或人完全没有的作用，避免装备起来之后塔变成摆设。现代人虽然最终能战胜恐龙，但那必须是**很后面很后面**的事。
- **[远期] 战争迷雾**：视野遮蔽本身（看不见的区域）留待本节。其中"来袭时机随机"部分已在 v0.2 第 1 节落地，配套的预警层次（声音 / 动物感知 / 雷达 / 地图）同属 v0.2。
- **[远期] 恐龙随机目标寻址**：v0.1 的恐龙只沿单一固定路径直指核心。后续应让恐龙拥有**随机目标**，而不是全部针对核心的同一条路径。
- **[远期] 会离开路径追击现代人的恐龙种类**：v0.1 维持设计文档"路径固定、永不绕路"。后续可引入个别会脱离路径主动追击现代人的恐龙，追出一定距离后放弃并返回路径。依赖：脱离固定路径的寻路（NavigationAgent3D）。
- **[远期] 昼夜循环与夜行生物**：实时化之后，白天黑夜应当成为最核心的玩法区别——**黑夜有别的远古生物进攻，需要不同的防御机制与生存方案**（具体防御形式尚未定义）。原计划放入 v0.2，现推迟到本节，以保证 v0.2 只做 6 项。
- **[远期] 建筑与科技升级链**：
  - 住房升级链：茅屋（Hut）→ 木屋（Wood House）→ 营房（Barracks）。原设计中该链的作用是提升 AP 上限，AP 已取消，**新作用尚未定义**，需先定再做。
  - 多资源复合建筑成本：高级防御建筑（如重型弩炮、加固石墙）引入石头与木材复合消耗。
- **[远期] 多样化恐龙种类与 AI 行为**：
  - `big_theropod`（大型兽脚类，高 HP、高攻城伤害、优先锁定沿途建筑）与 `pterosaur`（翼龙，飞行、无视地面木墙直扑船舱）的数值**已写在 `Config.DINOS` 里**，但 [WaveManager.gd:229](scripts/core/WaveManager.gd:229) 目前只生成 `raptor`——两种恐龙从未出场。接通它们是成本极低、收益很高的一步。
- **[远期] 视效、交互与摄像机控制**：
  - 自由斜俯视摄像机控制（Q/E 或中键旋转解决遮挡、滚轮缩放）；
  - 建筑受击特效：受击闪白与碎屑方块飞溅（血条本身已在 v0.2 第 7 项落地，本条只剩特效部分）。
- **[远期] 人的升级系统**：可以通过波次掉落收集到一些肉，吃了多次食物之后可以产生精力，精力可以用来升级人。目前想到可以有多个升级方向：**恐龙训化**（多一个能力，和恐龙和谐共处）、**超人**（各方面能力增强）、**建筑大师**（多一个能力，造的建筑会更强）。**待定**：“多一个能力”怎么进一步玩还没想到；能不能选多个能力、还是只能选一个，也未定。
- **[远期] 建筑的升级系统**：类似于防御塔这种强大的东西，不应该就有个木头到了就能造了，应该有一个需要升级的系统来解锁。**待定**：解锁方式还没想好。
- **[远期] 木尖刺的随机形象**：木尖刺朝向的方向可以做多个模型，每次布局**底座大小、形状、碰撞体积完全一样**，但形象可以随机，这样一排木尖刺显得更生动。当前 `Building.make_body` 的 "spikes" 分支按固定间距摆一排圆锥，**每根锥体就是这条的接入点**——换成随机挑选的模型即可，间距、碰撞盒、格子占用都不必动（硬规则：随机只允许改外观，不允许改任何参与碰撞或寻路的尺寸，否则同一道栅栏会因为随机而拦得住或拦不住）。
- **[远期] 自动化工人 / 运输机器人**：派遣自动工人或机器人前往野外运送物资。**注意：本条与 v0.2 第 3 项的"照料机制"直接互斥**——若建筑可由自动工人维持，照料就失去意义，两者只能保留一个。

---

## 版本号构建与注入规范

**版本号只在一个地方维护：`project.godot` 的 `application/config/version`。**

此前 `version.json`、`project.godot`、`AppInfo.VERSION` 三处各写一份，发版时要记得改三遍——结果是 v0.3 已经开发时三处仍然都写着 v0.2。而且**签入仓库的 `version.json` 优先级最高**，它一旦过期就会静默压过项目设置，这种错误从表现上和"真的就是那个版本"完全一样。

现在的规则：
1. **仓库里改这一处**：`project.godot` 的 `application/config/version`；
2. **构建时注入**：流水线可写出根目录 `version.json` 覆盖它（该文件已加入 `.gitignore`，**不再签入仓库**）；
3. **代码兜底**：`AppInfo.VERSION` 固定为 `"unknown"`——**刻意不是一个版本号**。一个看起来合理的字面量只是第四个会忘记更新的地方，而过期的它无法与真相区分。
4. **测试从 `ProjectSettings` 读取**，不复述字面量，所以升版本不需要改任何测试。

[`scripts/core/AppInfo.gd`](scripts/core/AppInfo.gd) 的解析优先级：
2. **构建时注入支持**：
   - CI/CD 构建流水线可通过生成或覆盖根目录下的 `version.json` 自动生效；
   - 或通过 Godot 命令行 `--export` 传递 `application/config/version` 配置覆写；
3. **界面展示**：HUD 顶部状态栏右上角及结算界面已自动绑定并展示当前版本号。

# 2D 界面精修计划 —— **已延后**

> **这份不是当前的优先级。** 主计划是 [SCENE-POLISH.md](SCENE-POLISH.md)：实际游戏体验、3D 场景。
>
> 这里的 20 条任务仍然有效，等场景做完再回来派。唯一的例外是 **T1（建立 Theme 资源）**——项目里至今没有任何 Theme，样式是几十处散装覆盖。哪天要动界面，那条仍然必须最先做。

---

# 界面精修计划（原文）

> 目标：从"能跑的原型"到**可以发布的界面**。
>
> 这个文件是**交接单**，写法和 [AGENT-TASKS.md](AGENT-TASKS.md) 一样：每条任务冷启动可做、只碰一小块地方、做完能自证。**一次派一条。**
>
> 先读第 0 节和第 1 节，它们是所有任务共同的规矩。**跳过它们做出来的东西一定会被打回。**

---

## 第 0 节：为什么现在看起来像学生作品

不是"美术不够好"这种笼统的问题。是五件具体的事，每一件都可修：

1. **整个项目没有 Theme 资源。** 所有样式都是几十处散落在脚本里的 `add_theme_font_size_override` / `StyleBoxFlat.new()`。没有统一的颜色、字号、间距、圆角，所以每块 UI 都长得不太一样——**这正是"业余"最直接的来源**。也意味着在建立 Theme 之前，任何"精修"都只是再加一处散装覆盖。
2. **用的是 Godot 默认字体。** 默认字体是引擎的调试字体，不是给游戏用的。换字体是单条性价比最高的改动。
3. **没有图标，全是文字。** "Wood: 0 / Stone: 0" 是调试输出的写法，不是界面。
4. **可操作的东西和背景长得一样**（见第 1 节）。
5. **3D 模型没有贴图**，全是平面着色的纯色几何体。这条有天花板，见第 4 节，别在上面花不该花的力气。

---

## 第 1 节：可读性法则（每条任务都要守）

> **玩家必须在零点五秒内看出：什么能操作、什么是背景。**

现在做不到：人、恐龙、建筑和草丛蕨类用同一套色调、同样的明度、同样的细节密度，全都静止不动。这是**界面问题不是美术问题**，而且它比"好不好看"更要紧——好看但分不清，游戏就是坏的。

三条法则，任何任务不得违反：

1. **背景永远低饱和、低对比。** 地面、草、蕨、石头、丘陵、远景——一律压在低饱和区间。它们是舞台，不是演员。
2. **可操作单位永远带一个恒定标记。** 不是只在选中时才有。人和恐龙脚下要有**接地环或投影标记**，让它们在任何背景上都能被一眼挑出来。这一条是第 2 节几条任务的全部内容。
3. **状态用形状和位置说，不要只用颜色。** 有色觉障碍的玩家占男性 8%。红绿血条 + 红绿"可造/不可造"是最常见的失败。

---

## 第 2 节：共同规矩

**项目宪法**（和 AGENT-TASKS.md 第 0 节相同，这里只列界面相关的增补）：

- **所有数值进 `Config.gd`**，包括颜色、字号、间距、圆角、动画时长。界面数值统一进 `Config.UI` 或新的 `Config.THEME`。
- **所有文字进 `translations/strings.csv`**，en + zh_CN 两列都要给。
- **测试从 Config 推导，不复述数值。**
- **0 条断言的测试算 FAIL**（runner 会拦）。

**每条任务的收尾，三样缺一不可**：

```bash
# 1. 全量测试必须全绿
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --script res://tests/test_runner.gd

# 2. 出图。改了界面而没有截图看过，等于没做
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --path . --resolution 1600x900 --script res://tools/playtest.gd

# 3. 开图冒烟，不许有报错
"C:/Users/jobzk/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . --quit-after 400
```

改了 `strings.csv` 或新增 `class_name` 之后**必须**先跑 `--headless --editor --quit` 重建缓存。

**分支**：从 `main` 开，名字写在每条任务里。**不要 push，不要合并。**

---

## 第 3 节：任务表

**T1 和 T2 是地基，必须最先做完，而且必须按顺序。** 其余每条都独立，可以任意顺序派。

| # | 任务 | 层 | 依赖 | 分支 |
|---|---|---|---|---|
| T1 | 建立 Theme 资源与设计令牌 | 地基 | — | `ui-theme` |
| T2 | 换掉默认字体 | 地基 | T1 | `ui-font` |
| T3 | 可操作单位的恒定接地标记 | 可读性 | — | `ui-actor-marker` |
| T4 | 悬停高亮（描边） | 可读性 | T3 | `ui-hover` |
| T5 | 背景降饱和 / 前景提饱和 | 可读性 | — | `ui-value-separation` |
| T6 | 世界内文字标签重做 | 可读性 | T1 | `ui-world-labels` |
| T7 | 顶栏：资源区 | HUD | T1 T2 | `ui-topbar-resources` |
| T8 | 顶栏：血量与核心状态 | HUD | T1 T2 | `ui-topbar-vitals` |
| T9 | 顶栏：速度与暂停控件 | HUD | T1 T2 | `ui-topbar-controls` |
| T10 | Option 面板外框与布局 | HUD | T1 T2 | `ui-panel-frame` |
| T11 | 建造菜单按钮 | HUD | T10 | `ui-build-buttons` |
| T12 | 建造预览 ghost | HUD | T5 | `ui-build-ghost` |
| T13 | 血条与施工进度条 | HUD | T1 | `ui-bars` |
| T14 | 来袭预警横幅 | HUD | T1 | `ui-raid-warning` |
| T15 | 胜负结算面板 | HUD | T1 | `ui-gameover` |
| T16 | 暂停菜单与设置 | HUD | T1 | `ui-pause-menu` |
| T17 | 资源图标 | HUD | T1 | `ui-icons` |
| T18 | 光照与后处理复调 | 3D | — | `ui-lighting-pass` |
| T19 | 模型材质：粗糙度与法线 | 3D | — | `ui-materials` |
| T20 | 接地阴影 | 3D | — | `ui-contact-shadows` |

---

## T1 — 建立 Theme 资源与设计令牌

**分支**：`ui-theme` ｜ **这是所有界面任务的地基，必须最先做**

### 目标

现在**项目里没有任何 Theme 资源**，样式全是散落在 `HUD.gd`、`OptionPanel.gd`、`PauseMenu.gd` 里的 `add_theme_*_override` 和临时 `StyleBoxFlat.new()`。所以每块 UI 的颜色、圆角、边框都略有不同，而且**没有一个地方可以统一改**。

建立一个 `Theme` 资源，把样式从代码里拿出来。

### 要改的文件

- `scripts/autoload/Config.gd`（新增 `THEME` 块：颜色、字号、间距、圆角、边框宽度）
- `scripts/ui/UiTheme.gd`（新建：从 `Config.THEME` 构造 `Theme`，注册到 `ThemeDB` 或挂在根 Control 上）
- `scripts/ui/HUD.gd`、`scripts/ui/OptionPanel.gd`、`scripts/ui/PauseMenu.gd`（删掉散装覆盖，改用 Theme）
- `tests/test_ui_theme.gd`（新建）

### 要做的事

1. **一套设计令牌进 Config**，不许在代码里出现裸颜色或裸字号：
   - 颜色：背景、面板、面板边框、主文字、次要文字、禁用文字、强调色、危险色、成功色
   - 字号：一个比例阶梯（例如 12 / 14 / 18 / 24 / 34），**不要每处随便取值**
   - 间距：一个阶梯（4 / 8 / 12 / 16 / 24）
   - 圆角、边框宽度、面板透明度
2. **一个 `Theme`，从这些令牌构造**。`Panel`、`Button`（normal/hover/pressed/disabled 四态）、`Label`、`ProgressBar` 都要有 StyleBox。
3. **删掉所有 `add_theme_*_override`**，除非那一处是真的需要偏离主题（那就在注释里写明为什么）。

### 验收

- 新建的测试断言：`Theme` 存在；它的每一项**等于 `Config.THEME` 里对应的值**（这条证明只有一个来源）。
- 断言 `HUD.gd` / `OptionPanel.gd` / `PauseMenu.gd` 里**不再有** `add_theme_font_size_override`（可以用 `FileAccess` 读源码断言，这是允许的）。
- 截图：顶栏、Option 面板、暂停菜单三处**边框、圆角、字号明显一致**。

### 不在范围内

- 不要改任何布局或位置（那是 T7–T16）。
- 不要换字体（那是 T2）。

---

## T2 — 换掉默认字体

**分支**：`ui-font` ｜ **依赖 T1**

### 目标

现在用的是 Godot 默认字体。它是引擎的调试字体，不是游戏字体，**这一条是单位工作量里观感提升最大的一项**。

### 要做的事

1. 选一款**免费可商用**的字体，**必须同时支持拉丁字母和简体中文**（游戏两种语言都要出）。思源黑体 / Noto Sans SC 是安全选择。放 `assets/fonts/`。
2. 通过 T1 的 Theme 设置默认字体，不要逐处覆盖。
3. **确认中文不出豆腐块**：切到 `zh_CN`，截图检查顶栏、Option 面板、暂停菜单。

### 验收

- 断言 Theme 的默认字体**不是** Godot 内置字体。
- 断言 `translations/strings.csv` 里的中文字符在该字体中有字形（抽查几个字，用 `Font.has_char()`）。
- 截图：英文一张、中文一张。

### 不在范围内

- 不要调字号（T1 已经定好阶梯）。
- 不要买字体，只用免费可商用的。

---

## T3 — 可操作单位的恒定接地标记

**分支**：`ui-actor-marker`

### 目标

**这条是第 1 节法则 2 的落地，也是整个计划里最重要的一条。**

现在人、恐龙和草丛蕨类混在一起，玩家看不出什么能点。选中环只在**已经选中之后**才出现——那时候玩家已经点中了，帮助来得太晚。

给每个**可操作/会动**的单位一个**永远都在**的接地标记。

### 要改的文件

- `scripts/fx/ActorMarker.gd`（新建）
- `scripts/entities/Hero.gd`、`scripts/entities/Dino.gd`（挂上标记）
- `scripts/autoload/Config.gd`（`THEME` 或新 `MARKERS` 块）
- `tests/test_ui_actor_marker.gd`（新建）

### 要做的事

1. 一个**贴地的椭圆**（decal 或极薄的 mesh），跟着单位走，永远可见。
2. **敌我必须一眼分开**：人和恐龙用不同颜色，而且**不要只靠颜色**——建议人是实心环、恐龙是断续/尖齿环，色觉障碍玩家也分得出。
3. 它**不能**被草盖住。标记要在草之上渲染（调 render priority 或略微抬高）。
4. 标记**不能**参与碰撞、不能上格子、不能挡点击。

### 验收

- 断言：人和恐龙**生成时就有**标记（不是选中后才有），且 `visible == true`。
- 断言：标记不在任何碰撞层上。
- 断言：人的标记和恐龙的标记**形状不同**（不只是颜色不同）。
- **截图（这条的关键验收）**：一张有人、有恐龙、有草丛蕨类的图，**眯着眼看也能立刻挑出哪两个是活的**。

### 不在范围内

- 不要碰选中环（它是"你点了这个"，和这条是两件事）。
- 不要改单位模型。

---

## T4 — 悬停高亮

**分支**：`ui-hover` ｜ **依赖 T3**

### 目标

鼠标移到可操作的东西上时，它要有反应。现在完全没有，所以玩家只能靠试。

### 要做的事

1. 悬停时给单位/建筑加一圈**描边**或提亮。
2. **只有真的能操作的东西才亮**——草、石头、地面不许有反应。这是在教玩家"什么能点"。
3. 光标也要跟着变（可造 / 不可造 / 可选中）。

### 验收

- 断言：悬停可选中物体时高亮出现，移开消失。
- 断言：悬停**背景装饰**（草、蕨、倒木）时**没有**任何高亮——这条比上一条更重要。
- 截图：悬停态和普通态各一张。

---

## T5 — 背景降饱和 / 前景提饱和

**分支**：`ui-value-separation`

### 目标

第 1 节法则 1。现在地面、草、蕨、丘陵和单位在同一个饱和度与明度区间，画面糊成一片。

### 要做的事

1. 把**所有背景元素**（地面、草、蕨、碎石、倒木、丘陵、远景）的颜色统一压到**低饱和**区间。数值进 Config，不要逐个 hardcode。
2. 把**演员**（人、恐龙）和**玩家造的东西**（木栅栏、防御塔、残骸）留在较高饱和度。
3. 用**明度**再分一层：演员比脚下的地明显更亮或更暗，任何背景上都不会糊。

### 验收

- 断言：`Config` 里背景色的饱和度**全部低于**演员色的饱和度（从 Config 推导，不复述具体数值）。
- **截图验收**：把截图转成灰度，**演员仍然能从背景里分出来**。这一条请真的做一次灰度检查再交。

---

## T6 — 世界内文字标签重做

**分支**：`ui-world-labels` ｜ **依赖 T1**

### 目标

现在资源点和建筑头上飘着白色 `Label3D`（"Stone 100/100"），没有背景板、没有层级、远近大小不一，**是画面里最"未完成"的东西之一**。

### 要做的事

1. 给标签加**底板或描边**，任何背景上都读得清。
2. **分级**：不是所有东西都要一直显示名字。建议只在悬停 / 选中 / 有异常（受损、被采空）时显示。**默认全显是调试视图，不是游戏界面。**
3. 距离衰减：远处的标签淡出，不要挤成一团。

### 验收

- 断言：默认状态下世界标签数量**明显少于**当前（从 Config 的显示规则推导）。
- 截图：默认视图 + 悬停某个资源点。

---

## T7 — 顶栏：资源区

**分支**：`ui-topbar-resources` ｜ **依赖 T1 T2**

### 目标

`Wood: 0  Stone: 0  Water: 0  Meat: 0  Bone: 0` 是调试输出。要做成一排**图标 + 数字**的资源条。

### 要做的事

1. 布局：图标在前、数字在后，等宽对齐（数字变化时不要跳来跳去——用等宽字体或固定宽度）。
2. **数字变化要有反馈**：增加时短暂高亮或 +N 飘字。
3. 资源为 0 时降低不透明度；不足以支付当前选中建筑时标红（但**不要只用颜色**，见法则 3）。

### 验收

- 断言：五种资源都在，顺序来自 `Config.RESOURCES`（不要在 UI 里重排）。
- 断言：数字宽度固定，从 0 变到 1000 时**布局不位移**。
- 截图：0 资源、满资源各一张。

### 不在范围内

- 图标本身是 T17。这条先用占位方块，把布局和行为做对。

---

## T8 — 顶栏：血量与核心状态

**分支**：`ui-topbar-vitals` ｜ **依赖 T1 T2**

### 目标

`Core HP: 10 / 10` 和 `Hero HP: 10 / 10` 现在是纯文字。核心（飞船残骸）掉血**是输掉游戏的唯一途径**，它应该是顶栏最醒目的东西，而不是和资源一样大小的一行字。

### 要做的事

1. 核心血量给**条 + 数字**，明显比资源区重。
2. 掉血时要有反应：条抖动、闪一下、数字变色。
3. 低血量要**升级警示**（例如低于 30% 时常驻脉动）。

### 验收

- 断言：血条比例来自实际 HP，不是写死。
- 断言：低血量阈值来自 Config。
- 截图：满血、半血、濒死三张。

---

## T9 — 顶栏：速度与暂停控件

**分支**：`ui-topbar-controls` ｜ **依赖 T1 T2**

### 目标

`Speed: 1x` 和 `Pause (Space)` 现在是两个默认按钮。做成一组紧凑的控件，并且**当前速度要看得出来**。

### 要做的事

1. 速度做成分段控件（1x / 2x / 4x 之类），当前档位高亮，不要做成"点一下换一个"的猜谜。
2. 暂停按钮图标化，快捷键提示做成次要文字。
3. 暂停时**全屏要有明确反馈**（边框、字样），不能只有按钮文字变了。

### 验收

- 断言：可选速度档来自 Config。
- 断言：暂停状态下有全屏指示。
- 截图：正常、2x、暂停三张。

---

## T10 — Option 面板外框与布局

**分支**：`ui-panel-frame` ｜ **依赖 T1 T2**

### 目标

右下角面板现在是一个深色圆角框，**下半部分是一大块空黑区**，标题和按钮之间的间距没有节奏。

### 要做的事

1. 面板**高度随内容自适应**，不要留一大块空。
2. 标题 / 状态 / 分隔线 / 按钮区，四段有明确的间距节奏（用 T1 的间距阶梯）。
3. 面板出现和消失要有**短促的过渡**（不超过 0.15 秒），不要硬切。

### 验收

- 断言：面板高度随内容变化（选中不同东西时不同）。
- 截图：选中人、选中建筑、选中资源点、未选中，四张。

---

## T11 — 建造菜单按钮

**分支**：`ui-build-buttons` ｜ **依赖 T10**

### 目标

建造按钮现在是默认按钮加一行文字。它要说清三件事：**造什么、要多少、现在造不造得起**。

### 要做的事

1. 每个按钮：名字 + 成本（按资源分列，带图标）+ 造不起时的明确禁用态。
2. 未解锁的建筑要说明**在等什么**（这个逻辑已经有了，见 `OptionPanel._missing_text`，别重写，只做视觉）。
3. 禁用态**不要只靠变灰**——加图标或角标。

### 验收

- 断言：成本来自 Config，不复述数值。
- 断言：造不起时按钮 `disabled == true` 且有非颜色的提示。
- 截图：造得起、造不起、未解锁三张。

---

## T12 — 建造预览 ghost

**分支**：`ui-build-ghost` ｜ **依赖 T5**

### 目标

现在的 ghost 是整体染绿/染红的半透明模型。**注意：它已经会按落点算出正确形状了**（`Wall.axis_at`，这是修过三轮的地方，不要动逻辑），这条只做视觉。

### 要做的事

1. 可放置 / 不可放置的区分要更强，且**不要只靠颜色**（加图标或格子边框样式）。
2. 加**格子指示**：让玩家看清会占哪一格。
3. 不可放置时说明**为什么**（占用 / 丘陵 / 资源点 / 造不起）——这个判断 `BuildSystem.can_place_building` 已经有了。

### 验收

- 断言：ghost 形状仍然等于落地后的形状（`tests/test_v04_terrain.gd` 的 test_17/test_18 必须继续全绿）。
- 截图：空地、占用格、丘陵三张。

---

## T13 — 血条与施工进度条

**分支**：`ui-bars` ｜ **依赖 T1**

### 目标

`StatusBar3D` 现在是一个带底板的小条。做得更精致，并且**血条和施工条要分得出来**（现在只有颜色不同）。

### 要做的事

1. 用**形状或位置**区分血条与施工进度条，不要只靠颜色。
2. 掉血要有**延迟追赶**的二段条（先掉白条再掉红条），这是让伤害看得见的标准做法。
3. 距离衰减：远处的条缩小或淡出。

### 验收

- 断言：满血时不显示（`FEEDBACK.health_bar_hide_at_full` 这条规则已有，别破坏）。
- 断言：施工条与血条在类型上可区分（不只是颜色）。
- 截图：受损建筑、施工中建筑、受伤恐龙三张。

---

## T14 — 来袭预警横幅

**分支**：`ui-raid-warning` ｜ **依赖 T1**

### 目标

预警是这个游戏节奏的支点（`RAIDS.warning_lead_time`），现在只是一行带 emoji 的文字。

### 要做的事

1. 做成有分量的横幅：倒计时、波次、强度提示。
2. 出现时要有**明确的动画**，但不能挡住战场。
3. 大波（`HUD_BIG_WAVE`）要和普通波**明显不同**。

### 验收

- 断言：倒计时来自 `Config.RAIDS`。
- 断言：横幅不遮挡可玩区域中心。
- 截图：普通波、大波两张。

---

## T15 — 胜负结算面板

**分支**：`ui-gameover` ｜ **依赖 T1**

### 目标

现在是"VICTORY!"加一行字加一个重开按钮。这是玩家一局结束唯一会仔细看的界面。

### 要做的事

1. 胜/负两种状态视觉上截然不同。
2. 给出这一局的**数据**（撑过几波、造了多少、采了多少）——数据 `GameState` 里已经有，不要新增玩法。
3. 重开按钮要显眼。

### 验收

- 断言：显示的数据来自 `GameState`，不是写死。
- 截图：胜利、失败两张。

---

## T16 — 暂停菜单与设置

**分支**：`ui-pause-menu` ｜ **依赖 T1**

### 目标

`PauseMenu.gd` 现在是一列默认按钮。它是玩家看到的第二个界面（第一个是游戏本身）。

### 要做的事

1. 按 T1 的主题重做布局与按钮。
2. 语言切换要**当场生效并且看得见**（`I18n` 已经支持，见 `locale_changed`）。
3. 背景要有遮罩，暂停状态一眼看得出。

### 验收

- 断言：切语言后菜单文字立即变化。
- 截图：英文、中文两张。

---

## T17 — 资源图标

**分支**：`ui-icons` ｜ **依赖 T1**

### 目标

五种资源 + 建筑类型都需要图标。**这条是纯资产任务**，可以和别的并行。

### 要做的事

1. 木、石、水、肉、骨五个图标，风格统一、单色或双色、在小尺寸下依然可读。
2. 建筑图标：木栅栏、防御塔。
3. SVG 或高分辨率 PNG，放 `assets/icons/`。

### 验收

- 断言：`Config.RESOURCES` 里每种资源都有图标，**没有遗漏**（这条能挡住以后加资源忘了加图标）。
- 截图：24px 下的图标排一行，看清不清。

---

## 第 4 节：3D 观感（T18–T20）——先读这段

**说清楚天花板在哪，免得在这上面花不该花的力气。**

现在所有 3D 模型是**用 GDScript 逐个三角形生成的**（`ModelLibrary.gd`、`GroundCover.gd`、`TerrainBuilder.gd`），没有用 Blender，没有用任何资产包，没有贴图——只有顶点色。

**这条路有硬天花板。** 代码能做好比例、剪影和颜色，做不出布料的织纹、鳞片的层次、金属的磨损。而"可以发布的写实 PBR"这几样恰恰是关键。

所以：

- **T18–T20 是在天花板以内把能拿的拿满**，值得做，成本不高。
- **真正要突破，只有换成真模型 + 真贴图**（买资产包是既定策略，见 VERSION.md v0.5）。接模型的管道**已经修好了**：`Config.VISUALS` 里把 `scene` 从 `""` 改成模型路径就行，`VisualLibrary` 会自动按声明尺寸装配好，**不需要改任何逻辑**。
- **界面（T1–T17）没有这个天花板。** 2D UI 用代码可以做到完全可发布的水准。**性价比最高的力气在那边。**

---

## T18 — 光照与后处理复调

**分支**：`ui-lighting-pass`

### 目标

`Config.ENVIRONMENT` 那套是按"写实 PBR"定的，但是在**模型还是平面着色**的情况下定的。现在有草、有植被、有模型了，该对着真实画面重调一遍。

### 要做的事

对着截图调，不要凭空调：太阳角度与色温、环境光强度、SSAO 强度与半径、雾的浓度、AgX 曝光。

**硬约束（有测试盯着，别碰）**：`fog_depth_*` 只在 `FOG_MODE_DEPTH` 下有效；`fog_density` 在两种模式下含义完全不同。见 `tests/test_v05_environment.gd::test_06b`。

### 验收

- `test_v05_environment.gd` 全绿。
- 截图：同一场景改前 / 改后对比。

---

## T19 — 模型材质：粗糙度与法线

**分支**：`ui-materials`

### 目标

所有模型现在共用一个 `GroundCover.cover_material()`：顶点色、粗糙度 1.0、无法线。所以**金属的残骸和潮湿的树皮反光完全一样**，这是"塑料感"的直接来源。

### 要做的事

1. 按材质分类给不同的粗糙度/金属度：船体金属、树皮、岩石、皮肤、树叶。
2. 用程序化噪声法线给大面积表面加细节（地面已经这么做了，照抄 `Main._noise_material` 的做法）。
3. 树叶要 `CULL_DISABLED` 且考虑次表面散射近似。

### 验收

- 断言：不同材质类别的粗糙度取值不同，且都来自 Config。
- 截图：残骸、树、岩石三张近景（用 `playtest.gd -- closeup`）。

---

## T20 — 接地阴影

**分支**：`ui-contact-shadows`

### 目标

东西看起来"浮"在地上。SSAO 有了但不够——小物件和地面的接触处需要更明确的暗部。

### 要做的事

1. 给单位和建筑加接地 AO（贴地的暗色 decal，或加强 SSAO 近距离权重）。
2. 检查每个模型是真的**踩在**地上（`anchor: "feet"` 是否都对）。

### 验收

- 断言：`Config.VISUALS` 每一项的 `anchor` 都是有意选的（复查一遍，不是照抄）。
- 截图：近景看接地。

---

## 加任务的格式

照上面的模板：**目标 / 要改的文件 / 要做的事 / 验收（含截图）/ 不在范围内 / 分支名 / 依赖**。

「不在范围内」和「依赖」两栏不是客套——它们是这个文件能并行派活的全部原因。
